# 09 · 我的冲刺一页纸（考前 30 分钟）

> 依据通用模板：`通用焚诀/05-场景题与售前答题/02-一页纸作战手册模板.md`。
> 只放"记不住就露怯"的内容；展开版都在 `02/03/06/07`，这里不重复。面试前 30 分钟只看这一页。

---

## 1. 定位一句话（开场 30 秒）
> 固定自我介绍模板见 `06` §1（逐字）。应急压缩版：**"AI 应用开发方向，两段企业级 Agent 项目：一个解决行业合规 SOP 怎么变成可执行可回归的 Agent 能力，一个解决多渠道对客的可靠执行——一直在做'让 Agent 在真实业务里稳定运行'这件事。"**

## 2. 雷点五条（先背这五条，两场面试真实翻车点）

1. **OneID**：显式绑定不靠猜——用户表↔渠道账号一对多，/pair 配对码 + 五级信任阶梯（configured/trusted_direct/explicit/auto_feishu/transfer_reset）+ 粘性归属。
2. **并发调度**：入口→按会话哈希路由→串行车道（四道闸门：终态丢弃/变更暂停/活跃哨兵/容量）→指数退避（5 次、5s 起）→双时钟判卡死→分级终止（2s/15s/强杀）。
3. **传输层口径**：项目实际 **WebSocket + stream_event**（别再说 SSE）；SSE 概念题：HTTP 长连接单向推 + Last-Event-ID 续传 + retry；断线恢复=服务端快照 + runId fencing + eventId 幂等。
4. **三层模型边界主动讲**：Profile 并发写仍可能冲突——乐观锁+写时合并，生产上行级锁/单写者。**主动说，别被问出来。**
5. **链路五段口诀**：接入→路由→Loop→权限→发送（别跳到权限答偏）。

## 3. 四账本去重键（王牌，必背）
| 账本 | 去重/幂等键 | 语义 |
|---|---|---|
| Inbox | UNIQUE(provider, account_id, external_message_id) | 入站全局去重 |
| TurnRuns | runId = sha256(logicalKey) | 创建即去重 |
| Outbox | UNIQUE(turn_run_id, ordinal) + payload_hash | 分阶段投递幂等 |
| StreamingCard | revision CAS | 每 turn 至多一张活跃卡 |

> 三句哲学：**先落库再动手 / 不确定即冻结 / 恢复尊重租约。** sending 后超时 = uncertain 冻结转人工（副作用不可知，宁可冻结不可重复）。

## 4. 数字锚点（每个都能讲来历，没有的别编）
| 锚点 | 来历 |
|---|---|
| 7 类 IM 渠道 | 简历口径（飞书/微信/QQ/钉钉/Discord/Telegram/WhatsApp + Web 第八渠道） |
| IMChannel 约 50 行 | 最小契约五个必须方法 + 可选能力方法 |
| 70ms/字符、≥1000ms | 飞书流式三级降级（打字机→CardKit→patch） |
| MAX_RETRIES=5、BASE_RETRY_MS=5000 | GroupQueue 指数退避 |
| 分级终止 2s/15s/强杀 | 卡死恢复 |
| 业务规模 | **【待确认——向导师要口径，见 00-A2】没有就明说** |

## 5. 每段经历一句话逻辑
- **获客平台（辰创）**：客户散、跟进断 → 销售 Workflow + 三层身份 + 四账本 + 串行调度 → 数字员工驻场七渠道，不重不丢、不越权。
- **养殖合规审查（梵域·八戒农牧）**：记录分散、靠人工交叉核查 → 六步审查 SOP + 工具契约 + PreToolUse 硬拦截 + Event Trace 回归 → 4 类任务 × 6 部门 72 个模板，风险报告初稿 + 人工审核。
> 两段不是业务上下游，是**同一条主线的两次演练**（横向可靠性 / 纵向行业深度），串法见 `16` §3。

## 6. 场景题走位（5 步）
① 先问 2 个澄清（最痛环节？数据在哪？）→ ② 定骨架（业务对象→数据→AI 能力→Agent→人工→闭环）→ ③ 选最小可评测场景 POC → ④ 安全阀（AI 初筛→人复核→留痕→高风险人工）→ ⑤ ROI 收尾。
> 迁移证据三件套随时挂：统一抽象 / 四账本+调度 / 权限模型（见 `05` 总表）。
> 完整 12 步锚定演练两道（获客数字员工 / 知识库问答）已备好——见 `05` 场景题演练区，开场必问 2 个澄清问题再走骨架。

## 7. 高频五问（20 秒答法）
1. Agent vs Chatbot → 回答 vs 执行（挂数字员工）。
2. 为什么不是一个全能 Agent → 角色/上下文/权限/责任边界拆分。
3. 怎么降幻觉 → 依据→约束→校验→兜底（挂四账本兜底）。
4. 上下文工程 vs Agent 工程 → 一个管"模型看到什么"，一个管"Agent 怎么行动"。
5. 数字化 vs 智能化 → 先流程上系统，再 AI 参与决策执行；交付常是"先系统再叠 Agent 亮点"。

> **「讲一个项目」速记**：主讲**蓬蓬AI · 养殖场合规与异常审查 Agent**（痛点→六步审查 SOP→工具契约+权限硬拦截+Eval 回归，挂八戒农牧 72 模板 + 生物安全成果样例）；获客平台做备选。90 秒版见 `06` §2.5。
> **「最难的问题」速记**：双端渲染不一致 STAR（复现→三处各自演进→AgentEvent 可辨识联合+契约测试进 CI）——完整版见 `06` §2.6。

## 8. 反问（选 2）
① 团队 AI 落地现在处于 MVP 验证还是往生产推？实习生能摸到真实链路吗？② 您带过的优秀实习生最共同特质？③ 前三个月最希望我先跑通的一件事？

## 9. 金句四句
1. 模型负责提议，系统负责裁决。
2. 宁可冻结，不可重复。
3. 统一的不是功能，是变化的维度。
4. 消息不重不丢不是调参调出来的，是账本记出来的。

## 红线（默念）
**永不编 → 口径不升格（应用端是我的，Runtime 是底座）→ 技术挂业务 → 主动讲边界优于被问出来。**
