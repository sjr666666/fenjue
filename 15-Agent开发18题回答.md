# 15 · Agent 开发 18 题回答

> 生成日期：2026-09-13 ｜ 依据资料：`01-画像与简历`、`03-拷打树`、`06-高频Q&A与金句`、`07-项目内参`、`09-冲刺一页纸`、`12-四板斧`、`13-选型原则`、`14-豪言豪语`
> 说明：这不是某场面试的复盘，而是**基于我的焚诀与简历资料，对这 18 个 Agent 开发问题的回答**。
> 每题结构：**结论（先给答案）→ 展开 → 我在项目里的对应实现 → 资料边界**
> 四段式口径统一：**解决什么问题 → 怎么实现 → 为什么这样设计 → 局限与扩展**

---

## 0. 先分清：哪些题我有第一手资料，哪些没有

| 题 | 性质 | 依据 |
|---|---|---|
| 1、2、3、7、13、15、16、17、18 | **第一手**（两个项目的实现） | 直接对应项目实现细节，可展开到源码级 |
| 5、6、10、12 | **半第一手**（设计层我做过，行业做法是通用补充） | 先给我在项目里的做法，再补行业通行手段 |
| 4、8、9、14（ASR 四题）、11（OpenClaw） | **通用工程理解 + 类比我的流式实践** | 我的资料里没有 ASR / OpenClaw 生产事实，按工程框架回答并标注，不编指标 |

**ASR 四题的统一前提：**
> 焚诀与简历里**没有任何 ASR 第一手资料**。以下回答基于通用的流式链路工程框架，并把我在流式事件流这一侧的真实实践（飞书流式卡片三级降级、StreamEvent 增量渲染与迟到事件丢弃）作为类比——它与 ASR 的"partial 先上屏、最终再纠正"是同一个问题。凡涉及具体延迟毫秒数、字错率数字，一律不给。

**OpenClaw 题的统一前提：**
> 我读过它的文档与生态，没有在生产部署过它本身；我的项目是同类架构的自有实现（Runtime 底座复用 Pi）。所以回答的是"它要补什么"，依据是通用工程判断 + 我在同类系统上补齐过的清单。

---

## 1. 简单做一下自我介绍

**结论：直接引用 `06` §1 的定稿版（约 450 字 / 90 秒），结尾按需补一句即可。**

> 我是小石，主要方向是 AI 应用开发，技术主线是 Agent 开发和 Agent 工程化。我目前有两个比较核心的项目实践。第一个是多渠道获客数字员工平台，面向企业销售和市场场景，我主要负责 Agent 应用端，把数字员工接入飞书、微信、Telegram 等 7 个 IM 渠道，重点解决多渠道接入、上下文连续性以及并发跟进这些实际业务问题。第二个是蓬蓬AI · 员工超级助手，面向农牧企业八戒农牧的市场和运营团队。我主要负责 Agent Runtime 这一侧，包括统一模型后端、AgentEvent 事件流、Electron 和 Web 的统一通信，以及工具权限和 MCP 能力接入，同时沉淀了查/算/写/讲 72 个工作模板与 8 大行业知识库。核心目标是让换模型不用改前端，多端使用同一套 Agent 能力，同时对敏感操作进行权限控制。这两个项目虽然业务场景不同，但我一直在做同一件事情：不是只把模型接进来，而是把 Agent 放进真实业务流程里，解决它怎么接入、怎么执行、怎么控制风险以及怎么稳定运行的问题。

**结尾按需补一句（三选一）：**
- **技术/工程向（联想 Agent 岗默认选这条）**：这个方向和贵司岗位里"从 MVP 验证到系统落地"的链路是同构的，我希望把这段工程化经验直接用在团队的业务场景上。
- **落地/业务向**：我一直相信 AI 落地的关键不在模型多强，而在能不能翻译成业务同事用得起来的东西，这正是我想在贵司做的事。
- **若岗位涉及语音/端侧交互**：我在流式事件流和多端一致上花的功夫最多——流式输出的中间态怎么渲染、迟到事件怎么处理、断线怎么恢复，这套东西在语音交互链路上是直接可迁移的。

**注意：** 这段控制在 90 秒以内（约 450 字）；项目只点到"做了什么、解决什么"，细节留给追问，不在开场铺开。

---

## 2. 讲 Agent 项目全链路架构的核心组件

**结论：分十层讲，串起来的是"事件流"这一条主线。**

| 层 | 组件 | 我项目里的对应 |
|---|---|---|
| ① 接入层 | 多渠道/多端适配 | IMChannel 统一契约（7 类 IM + Web 第八渠道）、统一 RPC（IPC ↔ WebSocket） |
| ② 身份与会话 | 身份/边界/执行记录分离 | Profile / Workspace / Runtime Session 三层 + identity_hash 可审计 |
| ③ 意图与编排 | 意图识别、任务拆分、Skill 装配、Agent Loop | 命令管线前短路、BaseAgent turn 前置（Skill 解析→校验→prerequisite→上下文注入→directive） |
| ④ 模型层 | 多模型/多后端适配、模型分级 | AgentBackend 统一契约 + ClaudeAgent/PiAgent 适配，换模型不改页面 |
| ⑤ 能力层 | Tool / MCP / API / 本地能力 + 权限硬拦截 | McpClientPool（连接/Discovery/Cache/Routing）+ PermissionManager（Safe/Ask/Allow-All + 六步管线） |
| ⑥ 知识与记忆 | 知识库检索、长期记忆、上下文预算 | 8 大行业知识库 + 72 模板（RAG 检索）、画像 Memory（fact/decision/lesson/open_loop + HMAC 签名 + CAS）、三层上下文预算（50K 告警 / 100K 抛错） |
| ⑦ 可靠性层 | 去重/幂等/状态机/队列/恢复 | 四账本（Inbox/TurnRuns/Outbox/StreamingCard）+ GroupQueue 串行调度 + 指数退避 + 分级终止 |
| ⑧ 安全与审计 | 权限矩阵、最小权限、留痕 | ACL 双维度授权（无 admin 旁路）、资源级 404 反枚举、七命令强制 Owner 门控、trust 包裹防注入 |
| ⑨ 可观测与评测 | Trace、事件流、Bad case 回流 | StreamEvent/AgentEvent 平铺信封（可回放）、契约一致性测试进 CI、失败回流调人设与模板 |
| ⑩ 承载层 | 应用形态与产品状态 | Electron / Web 双端、JSONL 会话持久化、产品状态（Loading/Partial/Error/Retry，不白屏不假完成） |

**一句话总结：**
> "这十层里，把各层串起来的其实是事件流——接入层的 StreamEvent、模型层的 AgentEvent、承载层的统一 RPC 推送，同一条主线。我在这个项目里最深的体会是：Agent 架构的核心不是模型那一层，而是模型两侧的'输入怎么进来、输出怎么出去、出错怎么收场'。"

**注意：** 别一上来讲"用了 LangGraph/多 Agent"；我的是**应用端工程化**架构，Runtime 底座是团队/开源（Pi），不升格。

---

## 3. Skill 和普通 function call 的区别

**结论：Tool / function call 是"动作"，Skill 是"受控能力包"——粒度、状态、上下文三个维度都不是一回事。**

| 维度 | Function Call / Tool | Skill |
|---|---|---|
| 粒度 | 原子动作：读文件、查库、发消息 | 一类任务：怎么把这件事从头做对（SOP） |
| 状态 | 无状态、单次调用、输入输出 Schema 明确 | 有流程：多步骤、可组合多个 Tool、有前置条件与失败分支 |
| 资产 | 只有函数签名 | 还有指令、模板、脚本、示例、质量线、负例 |
| 上下文 | 常驻在工具表里 | **按需加载**（渐进式披露） |
| 目的 | 让模型"能做" | 让模型"稳定地、可复用地做对" |

**一句话区分：**
> "Tool 回答的是'我能不能做这个动作'，Skill 回答的是'这类任务以后都该怎么干'。一个是函数，一个是作业指导书 + 配套工具 + 验收标准。"

**挂我的项目：**
- **Skill 是 turn 级装配，不是一次调用**：我的 BaseAgent 在每个 turn 前置里做——解析 Skill → 校验 → 注册 prerequisite（前置条件）→ 构建分支上下文 → 构建 Skill directive → 得到 effectiveMessage，最后才进 chatImpl()。这说明 Skill 是"进入模型之前的一次能力装配"。
- **业务侧的 Skill 化沉淀**：八戒农牧的 72 个工作模板（查/算/写/讲 × 6 部门 × 3 序号），每个都有场景定位、典型输入、典型输出——这就是把岗位经验从"每次临场推理"变成"可复用能力包"。
- **Skill 不是只有提示词**：内参 R16 有 Skill 六层优先级与清单指纹——说明 Skill 是有来源分级与变更可审计的资产。

**注意：** 别只说"Skill = 多个 function call 组合"，要补"受控 + 可沉淀 + 有质量线 + 按需披露"这四点，否则答浅了。

---

## 4. 流式 ASR 延迟的影响因素与降延迟手段

**结论：先把延迟拆成五段，再逐段优化；体验延迟和正确率要分离处理。**

### 影响因素（五段分解）
1. **采集与前端信号处理**：麦克风采集缓冲、采样率、AEC（回声消除）/AGC/ANS 算法的 lookahead 与帧长（通常 10–40ms 量级，算法链一长就叠加）。
2. **分包与传输**：音频帧长（10/20/40/60ms）、编码（Opus 帧大小）、首包建连、网络 RTT 与抖动、协议（WebSocket / gRPC 流式 / HTTP2）、弱网重传。
3. **VAD 与端点检测**：起点检测（漏触发 = 丢字，误触发 = 噪声进识别）；**尾点判停的静音超时直接加在尾字延迟上**（常见 300–800ms，这是用户感知最强的那段）。
4. **模型与解码**：流式模型结构（chunk-based Conformer / Transformer-Transducer / 流式 CTC）与 chunk size、right context（lookahead）大小、encoder 状态是否复用、beam size、热词偏置与 LM rescoring（**二遍 rescoring 是纯加延迟**）、GPU 排队与 dynamic batching 的拼批等待窗口。
5. **后处理与呈现**：增量标点、ITN（数字/日期/金额归一）、领域纠错、partial 结果的上屏与回改、终端渲染策略。

### 降延迟手段（对应五段 + 两个架构杠杆）
- **先量化再优化**：把端到端拆成「首字延迟 / 尾字延迟 / RTF」三个指标测出来，定位瓶颈段，别一上来调模型。
- **采集/传输**：缩短音频帧与缓冲、算法用低延迟模式、长连接复用 + 就近/边缘接入、二进制帧、弱网自适应（降码率/冗余）。
- **模型/解码**：小 chunk + 有限 lookahead、流式 transducer/CTC、encoder 状态复用避免重算、限制 dynamic batching 的最大等待窗口、热词偏置做在解码内（而不是二遍）、小模型做首遍。
- **交互侧（最容易被忽略）**：**partial 先上屏**——体验延迟可以远小于最终延迟；流式标点与 ITN 增量输出；VAD 尾点自适应（安静环境短、噪声环境长）；提供"说完了"按钮让用户主动结束。
- **架构杠杆一：两段式**——流式小模型快速出 partial 保体验，音频结束后用非流式/大模型做最终纠正保准确。
- **架构杠杆二：端云协同**——端侧做唤醒、端点检测与轻量识别，云端做完整识别。
- **Agent 侧的隐藏收益**：在 ASR 还在出 partial 时，Agent 就可以并行做意图预判、知识预热、工具预取——把"听"和"想"的时间重叠起来。

**我的对应实践（流式这一侧，是真做过的）：**
> ASR 算法我没做过，但流式交互这一侧我在项目里处理过同类问题：飞书流式卡片做了三级降级——Level0 打字机 70ms/字符、Level1 CardKit 全量替换 ≥1000ms、Level2 传统 patch，本质就是**根据通道能力选择'增量上屏还是整块替换'**；同时用 LRU 去重缓存（1000 条/30 分钟）防渠道重复推送，用 RunStreamFence 把 A 取消后迟到的输出丢弃，避免污染 B。这些和 ASR 的'partial 上屏 + 最终纠正 + 迟到结果丢弃'是完全同构的工程问题。"

**边界（必须主动说）：** ASR 无第一手生产经验，不报任何具体延迟数字（不编"从 800ms 降到 300ms"）；只讲分解框架与可迁移的流式工程经验。

---

## 5. 上下文压缩、embedding 检索不足与任务干扰规避

**结论：三件事其实是一个问题——上下文里该放什么、放多少、谁的别放进来。**

### (a) 上下文压缩
- **四种手段**：① 滑动窗口 + 分层摘要（滚动摘要 / 分段摘要，保留结论不保留过程）；② **结构化外置**——把事实、决策、待办抽到 Memory，上下文里只留指针；③ 工具结果裁剪——只留摘要 + 引用 + 必要时按需再取原文；④ 去重与合并（重复工具调用只留最后一次结果）。
- **三条原则**：
  1. 先定"什么绝不能被压"——系统约束、当前任务目标、未完成的待办、关键决策与安全约束、最近若干轮。
  2. 被压掉的必须**可恢复**（有 Trace / Memory 能取回），否则压缩就是丢信息。
  3. 压缩不是越狠越好，判据是**证据完整性 vs 预算**的平衡（第三板斧的治理问题）。
- **挂我的项目**：三层上下文预算（50K 告警 / 100K 抛错）是硬闸门；Profile/Workspace/Session 三层分离 = 记忆外置而不是堆上下文；画像 Memory 的 fact/decision/lesson/open_loop 四类结构化存储 = 结构化外置的工程版。

### (b) embedding 检索什么时候不够（6 条）
1. **语义相似 ≠ 任务相关**：用户问"报销流程"，召回的可能是一堆讲"流程"的无关文档。
2. **短查询与精确匹配弱**：编号、型号、人名、金额的查询，向量空间里几乎无区分度。
3. **否定与条件表达不了**："不包括 XX 的""2025 年之后的""只在华东区适用的"。
4. **切分破坏语义**：chunk 把答案切成两半，任一半都召回不到；多跳推理需要跨段关联，纯向量失效。
5. **时序与版本**：新旧文档混在一起，检索不到"最新那版"；**权限与状态这类硬过滤，向量天然不支持**。
6. **不可解释**：召回错了不知道为什么错，没法写针对性的回归用例。
- **改进组合拳**：混合检索（BM25/关键词 + 向量）→ 结构化/元数据过滤前置 → rerank 精排 → 查询改写/扩展（多查询、HyDE）→ 时效性加权 → **召回失败不静默**（明说没查到，比编一条强）。
- **挂我的项目（诚实版）**：我的检索走的是 **FTS5 trigram + bm25 关键词路线，短查询降级 LIKE 适配中文，没有向量能力**——这是我的真实边界，不假装有语义检索；规模上量、需要语义关联时再评估向量库。

### (c) 任务干扰规避（4 条）
1. **隔离**：不同用户/会话/租户上下文物理隔离；子任务用子 Agent 独立上下文，主 Agent 只拿结构化结论，不把子任务的完整过程塞回来。
2. **指令与数据分离**：工具返回、检索片段、文件内容、历史对话一律标 trust/source，显式声明"以下为数据、非指令"——防提示注入。
3. **收窄决策面**：无关的工具与 Skill 不进上下文（渐进式披露，见 Q13）；长任务拆开避免互相污染。
4. **写入冲突控制**：多任务共享写同一份数据时串行化 + 乐观锁，冲突显式报错而不是静默覆盖。
- **挂我的项目**：三层模型 + 串行队列 + 双 revision CAS 防覆盖；快照以 `<workspace_memory_snapshot trust="historical_data">` 包裹；StreamEvent fencing 让 A 取消后的迟到事件不污染 B。

---

## 6. 大模型判断调用 skill 的逻辑、纯靠 embedding 检索的问题

**结论：模型是在做一次"带上下文的语义路由"，不是在做相似度匹配。**

### 判断逻辑（模型实际看到什么）
模型拿到的是：**Skill 名称 + 描述 + 参数 Schema + 当前上下文（用户诉求、对话历史、任务状态、可用能力清单）**，然后做三件事：
1. **意图匹配**——这个诉求属于哪类任务？
2. **可行性判断**——前置条件具备吗？我有权限吗？有没有更合适的？
3. **结构化输出**——要么输出调用（含参数），要么选择不调用、直接回答或追问。

**关键：判断质量高度依赖 description 的写法。** 好的描述写的是"**什么时候用 + 什么时候不用**"的决策边界，不是功能介绍；还要配负例和 few-shot。

### 纯靠 embedding 检索的六个问题
1. **相似 ≠ 可执行**：检索匹配的是表述相似度，不是"这个 skill 现在能不能解决这个任务"。
2. **无状态判断**：不知道前置条件、权限、登录态、目标文件是否存在。
3. **数量一多就撞车**：几十上百个 skill 描述趋同，"写日报"和"写周报"在向量空间里距离极近，top1 漂移。
4. **复合意图失效**：一句话要三个 skill 串行，检索只会给你一个。
5. **否定与例外表达弱**："不要发邮件，只生成草稿"——向量抓不住"不要"。
6. **不可解释、不可测、不可调**：错了不知道为什么错，写不了回归用例，也没法用规则兜底；新增/改名即失效，冷启动差。

### 我的做法：五段式组合路由（不是二选一，是分层）
> **embedding/关键词粗排（低成本 narrowing）→ 硬过滤（权限、前置条件、能力可用性）→ LLM 精排（候选清单 + 负例 + few-shot）→ 低置信不调用、先澄清 → 调用前 Schema 校验 + 权限裁决、调用后结果校验 + 失败回流**

**挂我的项目：**
- BaseAgent turn 前置的 **Skill 解析 → 校验 → prerequisite 注册** 就是"前置条件硬过滤"这一层。
- **权限硬拦截**体现的是"模型提议、系统裁决"——模型可以想调，但能不能执行由 PermissionManager 用代码决定，绕不过。
- Skill 清单有**六层优先级与清单指纹**（R16），清单变更可审计。

**注意：** 结论不是"embedding 没用"，而是"它只能做粗排"——它是最便宜的 narrowing 手段，问题是不能让它单独拍板。

---

## 7. 用户多诉求时 Agent 任务拆分的注意点

**结论：拆分的难点不是"怎么拆"，而是"拆完之后别串、别重复、别失控"。10 个注意点。**

1. **先判断是不是真的多任务**：有时是同一任务的多个约束（"写周报并发给张总"——写是一个任务，"发给外部"是风险动作，要单独处理）。
2. **澄清优先**：缺关键参数（发给谁、什么格式、截止时间）先问，不要自作主张并行跑。
3. **依赖识别**：画 DAG，能并行的并行，有依赖的串行；并行必须防**共享资源写冲突**（同一文件、同一条数据、同一外部对象）。
4. **粒度按产出物拆**：每个子任务要有一个**可独立验证的产出物** + 明确的输入输出 Schema，不按句子拆。
5. **风险分层**：外发、付款、删改生产数据这类高风险子任务**单独确认**，不与低风险任务批量放行。
6. **失败隔离**：一个子任务失败不拖垮全部，允许**部分交付 + 明确告知**；子任务上下文隔离，只回传结构化结论。
7. **幂等**：每个子任务带幂等键，重跑不产生重复副作用。
8. **预算与停止条件**：总轮次/Token/时间上限、单子任务重试上限、超时降级路径。
9. **用户视角可见**：计划清单展示给用户，进度流式可见，可中途打断/改需求。
10. **别过度拆**：一句话能干完的不要拆成 5 个子 Agent——能说清"这里不需要拆"本身就是判断力。

**挂我的项目：**
- 获客平台按流程拆四段（触达/破冰/跟进/画像沉淀），固定流程不用 Agent 的地方直说；
- 幂等键 runId = sha256(逻辑键)，重放命中同一行；
- 串行队列按 `{folder}#{agentId}` 划车道，同会话严格串行；
- 不确定即冻结——**宁可部分交付，不可重复触达客户**。

**一句话总结：** 拆任务的收益是并行与隔离，代价是协调与一致性；所以我的原则是能不拆就不拆，要拆就必须给每个子任务配幂等键和失败边界。"

---

## 8. VibeCoding 理解、非流式 ASR 线上是否可行

### (a) VibeCoding
**结论：它是原型与探索的杠杆，不是生产的交付标准。**

- **是什么**：用自然语言表达意图、让模型生成代码，人只看运行结果、不看 diff（"accept all"），本质是意图编程/结果导向编程。
- **价值**：把"想法 → 可运行"从天压到小时；让非工程角色也能做出能跑的东西。
- **四个真实风险**：① 不看 diff 就没有责任主体，AI 生成的危险命令/上传/依赖无人把关；② 可维护性与技术债；③ "看起来能跑"的隐蔽 bug；④ 依赖与供应链风险（装了不该装的包）。
- **我的立场（要有主见）**：**原型可以 vibe，生产必须守门**——AI 生成 + 测试/契约/CI 把关。我实际用 Claude Code / Codex 工作流 + TDD：AI 写，测试判，契约一致性测试拦漂移。
- **进一步的推论**：**VibeCoding 让"生成"变便宜，于是"验证"变贵**——权限、测试、可观测这些"非模型工程"的权重反而更高。这恰恰是我一直在做的那一层（PermissionManager 硬拦截、契约测试、事件流 Trace）。
- **挂我的项目**：契约一致性测试就是"AI 生成速度上来了，怎么保证不漂移"的答案——把漂移从运行时拦到开发期、编译期。

### (b) 非流式 ASR 线上是否可行
**结论：分场景。离线转写类完全可行（甚至更优），实时交互类不可行。**

- **可行的场景**（用户不期待实时上屏）：会议纪要、访谈整理、视频字幕生成、客服录音质检、录音笔转写、短语音指令（几十秒内）。
  - 而且**更准**：能用双向全局上下文、多遍解码、热词 rescoring、说话人分离；**更省**：可批处理、可拼大 batch、可错峰。
- **不可行的场景**：语音助手、实时字幕、需要打断与轮次检测的对话式交互——用户说完还要等整段处理，体验断裂，且完全拿不到 VAD / 打断 / turn-taking 能力。
- **工业界折中：两段式**——流式出 partial 保体验，音频结束用非流式做最终纠正保准确。**流式和非流式不是替代关系，是级联关系。**

**边界：** ASR 无第一手经验，这是通用工程判断 + 场景权衡框架。

---

## 9. 流式与非流式 ASR 核心差异

**结论：流式买的是"时间"，非流式买的是"准确与简单"。**

| 维度 | 流式 | 非流式 |
|---|---|---|
| 输入方式 | 边录边传、分块流式 | 整段音频 / 文件 |
| 核心延迟指标 | 首字延迟、尾字延迟、RTF < 1 | 整体处理时长（可数倍于音频长度） |
| 上下文 | 单向 / 有限 lookahead（left 大、right 小） | 双向全局上下文 |
| 准确率 | 同模型通常略低（首尾、专有名词更明显） | 更高（可多遍、可 rescoring、可 diarization） |
| 中间结果 | 有 partial，上屏后会被修正（有抖动） | 只有最终结果 |
| 交互能力 | 支持 VAD、打断、轮次检测、实时上屏 | 不支持 |
| 资源形态 | 长连接、有状态、并发连接成本高 | 批处理、易弹性、可大 batch |
| 失败恢复 | 断线需续传/重连、状态对齐复杂 | 整段重试，简单 |
| 后处理 | 增量标点/ITN，易抖动 | 全局标点/ITN/说话人分离，稳定 |
| 典型场景 | 语音助手、实时字幕、通话实时辅助 | 会议纪要、字幕生成、录音转写、质检复盘 |

**一句话：** "差异不在'准不准'，而在**要不要在音频还没结束时就给出结果**——一旦要，你就得用单向上下文换时间，并接受 partial 会被修正。"

**边界：** 同上，讲框架不编数字。

---

## 10. 主 Agent 意图识别方法、单轮分类器效果有限的原因

### 意图识别方法（五层，从确定到模糊）
1. **确定性优先**：命令/正则/结构化事件短路，不经过模型（我的项目：内置命令管线前拦截，永不到达 Agent）。
2. **模型语义分类**：给候选意图清单 + 每个意图的正例/负例 + few-shot，输出结构化意图（含置信度与缺失槽位）。
3. **上下文融合**：多轮共指消解（"也发给他"、"那改成明天"）、当前任务状态、用户画像、可用能力清单（**能不能做**也是意图的一部分）。
4. **低置信策略**：不猜——要么澄清提问（给 2–3 个选项），要么路由到安全默认（检索问答 / 转人工）。
5. **闭环**：判定错误进 bad case，回流到描述与 few-shot；新增意图优先改 prompt/描述（分钟级），不急着重训。

### 单轮分类器效果有限的七个原因
1. **单轮文本不含上下文**：省略、指代、追问全靠历史。
2. **意图随对话演进**：先问价、再比价、再下单，静态标签表达不了中间态。
3. **复合意图**：一句话多个诉求，单标签分类天然表达不了。
4. **长尾与分布漂移**：训练覆盖不到的说法，业务上功能后漂移。
5. **不知道"能不能做"**：权限、状态、数据是否存在——这些是**系统信息，分类器只看文本，拿不到**。
6. **无法表达"我需要问一句"**：分类器只能硬选一个标签，歧义时没有"澄清"这个出口。
7. **维护成本**：加一个意图要重标重训；改 prompt/描述是分钟级。

**挂我的项目：** 内置命令管线前短路（七个变更类 IM 命令强制 Owner 门控，不到达 Agent）；四段人设（IDENTITY/SOUL/AGENTS/TOOLS）把"什么时候做什么"写进约束层；低置信不猜——**uncertain 冻结转人工**，这在意图层和执行层是同一条哲学。

---

## 11. OpenClaw 开源框架落地生产要补的能力

**先一句话说清它是什么（证明你真了解）：**
> OpenClaw 是开源自托管的个人 AI Agent：以 daemon 跑在本机，接 WhatsApp/Telegram/Slack/Discord 等 IM，能执行 shell、读写文件、浏览网页；模型无关（Claude/GPT/Gemini/DeepSeek/本地模型）；能力以 Skill（一个 SKILL.md：YAML frontmatter + markdown 指令）为单位扩展，通过 ClawHub 市场分发；它用**渐进式披露**——会话开始只把紧凑的 skill 清单快照进 system prompt，命中后才加载完整指令。

**结论：它解决的是"个人助理的 happy path"，生产缺的是"多人、多租户、线上"那一层。八项：**

1. **安全与权限（最要命）**：ClawHub 是开放式市场、准入门槛低（社区研究称早期约 12% 的 skill 含恶意代码）。要补：Skill 准入/签名/审计、沙箱执行（容器 + 降权 + 挂载白名单）、危险命令永远审批、凭据按 Skill 隔离（不能给全部 token）、提示注入防护。
2. **身份与多租户**：现在是一个人/一个工作区，生产要三层隔离（Profile/Workspace/Session）+ 租户行级隔离 + ACL 最小权限 + 审计留痕。
3. **可靠性**：消息去重与幂等、显式状态机与**不确定态**、崩溃恢复与断线重连、串行调度与并发控制、重试语义（副作用不可知时禁止盲目重试）。
4. **可观测**：Trace / 事件流、每步可回放、指标与告警、bad case 回流。
5. **评测**：离线回归集 + 在线抽检 + 五维 Eval，让"改一句 prompt"敢上线。
6. **成本与限流**：Token 预算、并发上限、队列、模型降级路径。
7. **运维与合规**：部署升级灰度、备份迁移、密钥管理、数据留存与 PII、合规审计。
8. **产品化**：中间态可见、可中断可撤销、人工接管通道、多端一致（不白屏、不假完成）。

**我的对应实践：**
> "上面这些，恰好就是我两段实习实际补的那一层——四账本解决去重/幂等/不确定态，GroupQueue 解决并发与退避，PermissionManager 解决工具执行硬拦截，契约测试解决多端漂移，事件流解决可回放，人工裁决解决兜底。**开源框架给你的是'能跑通'，生产要的是'失败得体面'。**"

**边界（主动说）：** 我没有在生产部署过 OpenClaw 本身，我的项目是同类架构的自有实现（Runtime 底座复用 Pi）。这里讲的是通用工程判断 + 我在同类系统上补齐过的清单。

---

## 12. 用户写作习惯提取、避免误判临时噪声为长期偏好

**结论：核心是区分"用户这次要什么"和"用户一贯怎么要"——用可复现性 + 来源可信度 + 跨场景一致性三道闸。**

### 机制（7 条）
1. **三层存储分离**：Session（当次指令，不跨会话）→ Workspace（当前项目/团队口径，随项目走）→ Profile（跨会话长期偏好，**严格准入**）。
2. **证据门槛（防噪声的核心）**：进 Profile 必须有"跨 N 个不同会话 + 跨 N 天 + 非同一任务"的复现证据；只出现一次的先进 Workspace 候选池观察。
3. **来源分级**：显式声明（"以后都用这个格式"）权重最高 > 重复行为（多次同样修改）> 单次修改；"这次简短点"这种一次性要求明确标为临时。
4. **区分"用户偏好" vs "内容本身错了"**：用户改了输出，可能是内容不对而不是风格问题。要抽的是**改动的规律性模式**（反复把被动改主动、反复要求加小标题、反复要求去掉形容词），不是逐字记录一次改动。
5. **去噪清单**：排除转述、引用、粘贴进来的他人文本、模板样例、一次性节庆/活动文案、别人代发的口径。
6. **可撤销 + 可审计**：每条偏好带证据引用（哪次会话、原话）、时间戳、置信度；软删除留痕（tombstone，保留"谁在何时为何忘记"）。
7. **确认与衰减**：高影响偏好写入前 Ask 确认或标"待确认"；长期不用则衰减、定期复审；冲突时以新的显式声明为准并保留历史版本。

**挂我的项目：**
- **Profile / Workspace / Runtime Session 三层**就是为这件事设计的——身份、边界、执行记录分离，identity_hash 让变更可审计；
- 画像 Memory 分 **fact / decision / lesson / open_loop** 四类——**lesson 就是"教训与偏好"这一类**；
- 写入经 **HMAC-SHA256 一次性签名**防伪造防重放，双 revision CAS 防并发覆盖；
- 快照以 `trust="historical_data"` 包裹——**记忆是数据不是指令**，防注入污染。

**一句话总结：** "噪声和偏好的区别在于三件事：能不能复现、来源可不可信、跨场景一不一致——三条都过，我才写进长期层。"

---

## 13. Skill 渐进式披露的定义与提升稳定性的原因

### 定义（三层）
- **L1 目录层（常驻）**：Skill 名称 + 一句话描述 + 适用场景 → 组成"能力清单"进 system prompt。
- **L2 指令层（命中后加载）**：完整 SOP、步骤、输入输出约定、示例、质量线、负例。
- **L3 资源层（用到才读）**：模板文件、脚本、参考文档、数据样本——按需读取，读进来才占上下文。

> 类比：L1 是目录，L2 是翻到那一页，L3 是页脚引用的附件。**OpenClaw 也是这么做的：会话开始只快照 skill 清单，命中后再加载全文。**

### 为什么提升稳定性（6 条）
1. **控上下文长度**：50 个 skill 的全文档会让上下文爆炸，触发**注意力稀释**（lost in the middle），长上下文里模型反而更容易忽略关键指令。
2. **降误触发**：候选空间小，相似描述不再互相竞争（"写日报" vs "写周报"）。
3. **减指令冲突**：不同 skill 的详细指令不会同时生效、互相打架。
4. **提高路由准确率**：模型在"短描述清单"上做选择，比在"长文档堆"里做选择容易得多。
5. **成本与延迟**：只为真正用到的能力付 token。
6. **安全与审计**：敏感能力不常驻，命中才加载，且加载动作可被拦截与审计。

### 落地要点 + 它的代价
- 清单要**稳定且可审计**（带版本/指纹，变更可追溯）；
- 描述要写"**什么时候用 + 什么时候不用**"，带负例；
- 命中后要过**前置条件校验**（prerequisite）再加载；
- **代价**：命中判断一旦失败，整个能力就"不存在"了——所以 L1 描述质量与粗排/精排是命门，必须配**低置信澄清**兜底。

**挂我的项目：** 四段 Prompt（IDENTITY/SOUL/AGENTS/TOOLS）+ 三层上下文预算（50K 告警 / 100K 抛错）；BaseAgent turn 前置在命中 Skill 后，才把 Skill directive 注入 effectiveMessage；内参 R16 有 Skill 六层优先级与清单指纹（清单可审计）。

---

## 14. ASR 选型评估维度、不能只看字错率的原因

### 评估维度（六组）
1. **准确率**：CER/WER（**分场景**看：安静近场/远场/噪声/口音/方言）、专有名词与热词准确率、中英混读、数字/日期/金额、标点与 ITN 正确率。
2. **实时性**：首字延迟、尾字延迟、RTF，以及**并发下的尾部延迟**（P95/P99 比均值重要得多）。
3. **交互能力**：VAD 起点/尾点、打断与轮次检测、partial 结果的抖动程度、说话人分离。
4. **工程与成本**：并发与吞吐、部署形态（云/私有化/端侧）、计费方式、弹性与 SLA、SDK/协议成熟度、可观测性。
5. **领域适配**：热词/偏置、领域 LM rescoring、自定义词表、方言支持、能否增量迭代。
6. **数据与合规**：音频留存、PII、私有化、跨境、审计。

### 不能只看字错率的五个原因
1. **CER 是离线语料集上的平均值，掩盖长尾**：你的用户在噪声、远场、口音、方言里，测试集里没有。
2. **CER 衡量"字"，业务痛的是"义"**："三点"听成"三十分"，字面对了、语义错了；金额、人名、术语错一个字就是事故——真正要看的是**关键字段准确率 / 语义错误率（SER）**，不是字符错误率。
3. **CER 完全看不到实时性**：CER 低 1% 但首字延迟高 800ms 的模型，在对话场景直接不可用。
4. **CER 看不到交互能力**：VAD 判句错（截断用户的话 / 不判停导致长等待）、打断检测、说话人分离——都不在 CER 里，却决定体验。
5. **CER 看不到工程属性**：并发下的稳定性、P99 延迟、成本、私有化与合规、热词适配速度。

### 我的方法论（挂我的通用打法）
> "按场景定指标权重：会议纪要场景权重给准确率 + 说话人分离，语音助手场景权重给首字延迟 + 打断。然后**建自己的评测集**——真实场景音频 + 关键字段标注，跑端到端，而不是看厂商报告；线上看**采纳率 / 人工修改率 / 重复触发率**，而不是只盯离线指标。这跟 Agent 评测是同一条道理：不能只看任务成功率。"

**边界：** ASR 无第一手经验，讲方法不编厂商对比数据。

---

## 15. 主/子 Agent 模型分配、状态机设计要点

### (a) 模型分配：按「决策复杂度 × 出错代价 × 调用频率」三维分配
- **主 Agent**（意图理解、路由、与用户对话、处理歧义与拒答）→ **强模型**：这里错了，后面全错。
- **子 Agent**（抽取、分类、格式化、改写、检索判定、模板填充）→ **小/快/便宜模型**：任务窄、可验证、量大。
- **高风险动作前** → 强模型 + 人工确认；**高频低价值** → 弱模型 + 规则兜底。
- **最小权限**：每个子 Agent 限定工具集与输出 Schema（不给无关工具，输出必须是结构化）。
- **可降级**：超时/超预算/强模型不可用时降级到弱模型，结果标记为"降级产出"告知用户。
- **成本可观测**：按任务记 token，能定位是哪一类子任务在烧钱。

**挂我的项目：** AgentBackend 抽象（换模型不改上层）正是这套分配能落地的前提；Thinking Level 与推理强度随抽象流转（capabilities-driven UI）——**同一个页面能按任务调推理强度**。

### (b) 状态机设计要点（10 条）
1. **状态集合显式且封闭**：pending / running / waiting_tool / waiting_permission / paused / done / failed / aborted / uncertain，明确合法转移，禁止隐式跳变。
2. **终态明确，且 failed ≠ uncertain**：failed = 确定没做成（可重试）；**uncertain = 副作用不可知（禁止自动重试，冻结转人工）**。这是我项目里最重要的一条语义区分。
3. **状态持久化**：唯一可信状态源（我的是 SQLite），崩溃可恢复，**不把队列语义活在内存里**。
4. **幂等键 + 版本号/fencing**：过期 worker 的写入静默失配。
5. **超时与卡死检测**：双时钟（活动时钟 + IPC 债务时钟），分级终止（优雅 2s → docker stop 15s → 强杀）。
6. **重试语义**：只重试确定无副作用的动作；指数退避（MAX_RETRIES=5 / BASE_RETRY_MS=5000）+ 上限；**用户中断保留退避进度**（否则中断一次就让退避归零，陷入死循环）。
7. **事件驱动**：每次状态变更产出事件，可 trace、可回放、可推前端。
8. **可中断/可取消**（AbortSignal），取消后迟到结果用 fencing 丢弃。
9. **并行子任务的部分完成聚合**：允许部分交付，明确告知哪部分没成。
10. **可重放**：trace 能重放调试，这是评测与 bad case 回流的基础设施。

**挂我的项目：** 四账本状态机（pending→claimed→uploading→uploaded→sending→delivered + uncertain）、runId 幂等、GroupQueue 双时钟与分级终止、StreamEvent fencing、暖 Runner（跳过冷启动）。

---

## 16. Agent 多源输入的处理方式

**结论：统一信封 + 专用解析器 + 置信度一路带下去。**

### 输入类型
文本、语音（ASR 结果）、图片/截图、文件（PDF/Word/表格）、结构化事件（系统/设备/传感器/表单）、工具返回、历史记忆与画像、外部系统回调。

### 七条处理原则
1. **统一归一化成模态无关的信封（Envelope）**：source（来源）、modality（模态）、timestamp、traceId、confidence、trust 级别、原始内容指针。
2. **各模态走专用解析器**（ASR / OCR / 文档解析 / 结构化事件适配），结果统一进同一个 Event/Context 模型——**统一的是不变的部分（信封与语义），差异留在适配器里**。这和我的 IMChannel 是同一个套路。
3. **置信度一路带下去**：ASR 的 partial、OCR 的低置信区、表格的合并单元格都要标记；**低置信允许追问，不能当事实直接用**。
4. **指令与数据严格分离**：文件内容、网页、ASR 文本、历史对话一律标为数据并声明 trust——防提示注入。
5. **冲突消解有优先级**：结构化/系统来源（工具返回、数据库）> 用户口述 > 模型推断；时间戳新的覆盖旧的；冲突且无法判断就问。
6. **时序与并发**：多源异步进入同一串行通道（按会话/对象），避免并发写上下文；迟到事件用 fencing 丢弃。
7. **溯源**：每条进入上下文的内容带来源指针，输出可引用——这也是"证据"维度评测的地基。

**挂我的项目：** IMChannel 统一七渠道 + 前缀 JID 寻址（`provider:chatId#account:id#thread:id#root:id`）；AgentEvent 五类事件统一（文本增量/工具启停/权限请求/错误/完成）；画像快照 trust 包裹；低置信画像项标"待销售确认"**不自动生效**——这是我在场景题里定的规矩。

---

## 17. Agent 评估体系搭建、不能只看任务成功率的原因

### 三层体系
1. **离线**：场景化评测集（真实请求 + 期望产出/约束），每次改 prompt / skill / 模型都跑回归。
2. **在线**：埋点与指标（任务完成率、首字延迟、轮次、工具调用次数、成本、人工接管率、用户修改率/采纳率、重复动作率）+ 灰度/AB。
3. **人工**：抽检 + bad case 归因回流（五桶归因：缺材料 / 推理不闭合 / 证据不足 / 权限风险 / 格式不达标 → 对应回流到第一/第二板斧）。

### 五维指标（+1）
Outcome 结果 / Trajectory 过程 / Evidence 证据 / Safety 安全 / Cost 成本，外加 **Stability 稳定性**（同一输入多次运行的一致性，pass@k）。

### 不能只看成功率的六个原因
1. **掩盖过程错误**：靠错误路径碰巧答对（调了不该调的工具、绕过权限、编了内容），成功率一样 100%。
2. **掩盖体验**：延迟 30 秒、来回问 8 轮才完成——成功率 100% 但没人愿意用。
3. **掩盖安全**：越权一次、外发错人一次就是事故，成功率还是 99%。
4. **掩盖成本**：token / 调用次数翻倍，成功率不变但不可持续。
5. **掩盖证据**：答案对但没有依据、引用不可回溯——在知识类与合规类场景等于不可用。
6. **掩盖样本偏差**：评测集太干净；只看最终答案不看轨迹，就无法定位"到底该改哪一层"。

**挂我的项目：** 事件流本身就是 Trace（过程可回放）；四账本让每轮执行都有可回放状态（**这是过程评测的地基**）；回执丢失不盲目重试而走人工裁决——**把"不确定"变成可观测指标，而不是把它藏进失败里**；bad case 回流调人设与模板。

---

## 18. 区分生产可用 Agent 和演示 Demo 的方法

**结论一句话：Demo 跑的是 happy path，生产跑的是失败路径。**
**一句话总结：Demo 证明它能做到，生产证明它做错了也不会出事。**

### 九个自测/反问点（每个都挂我的实现）
1. **失败怎么表现**：会不会诚实失败？有没有不确定态与人工接管？→ 我的：`uncertain` 冻结整个 Turn 转人工裁决。
2. **副作用有没有护栏**：工具执行前有没有代码级硬拦截？→ 我的：PermissionManager + DANGEROUS_COMMANDS（rm/sudo/git push 永远审批，白名单绕不过）。
3. **重复与丢失**：有没有幂等键、去重、状态机？→ 我的：四账本（Inbox/TurnRuns/Outbox/StreamingCard）。
4. **崩溃与断线**：状态是不是唯一可信源？重启后会不会重复执行？→ 我的：SQLite 唯一状态源 + "先恢复状态 → 再开端口 → 最后连渠道"。
5. **并发与隔离**：多会话/多用户会不会串台？→ 我的：三层模型 + 串行队列 + 双 revision CAS（我踩过会话污染事故，用这个修的）。
6. **可观测**：能不能回放一条请求的全过程？→ 我的：事件流即 Trace，StreamEvent 平铺信封。
7. **回归与评测**：改一句 prompt 敢不敢上线？→ 我的：契约一致性测试进 CI + 五维 Eval + bad case 回流。
8. **成本与限流**：有没有预算、并发上限与降级？→ 我的：三层上下文预算（50K/100K）、模型分级与降级。
9. **长尾**：脏输入、超长输入、方言、网络抖动、第三方超时、**用户中途打断**——Demo 从不测这些。

### 再加三条"更成熟的标志"
- **数据与知识有治理**：知识有更新机制、召回失败不静默（宁可说没查到，不能编一条制度出来）。
- **运维闭环**：部署/升级/灰度/备份/审计都有路径。
- **敢说边界**：**能明确说出"这里不该用 Agent"**——普通检索/Workflow/规则更合适就直说，这是成熟度不是短板。

---

## 附：18 题结论速览

| # | 一句话结论 |
|---|---|
| 1 | 自我介绍沿用 `06` §1 定稿版，结尾按需补一句 |
| 2 | 十层架构，串起来的是**事件流**这条主线 |
| 3 | Tool 是动作，Skill 是受控能力包（受控/可复用/有质量线/按需披露） |
| 4 | ASR 延迟拆五段（采集/传输/VAD/解码/后处理），**partial 先上屏** |
| 5 | 压缩留"不能被压的"；embedding 不够就混合检索+rerank+硬过滤；干扰靠隔离+指令数据分离 |
| 6 | 模型做的是带上下文的语义路由；embedding 只做粗排，不能单独拍板 |
| 7 | 拆分难点是别串、别重复、别失控（幂等键 + 失败边界 + 风险分层） |
| 8 | 原型可以 vibe，生产必须守门；非流式 ASR 离线可行、实时不可行 |
| 9 | 流式买时间，非流式买准确与简单，工业界级联用 |
| 10 | 意图识别五层；单轮分类器没有上下文、无法澄清、不知道能不能做 |
| 11 | OpenClaw 给"能跑通"，生产要补：权限沙箱/多租户/可靠性/可观测/评测/成本/运维/产品化 |
| 12 | 偏好进长期层的三道闸：可复现 + 来源可信 + 跨场景一致 |
| 13 | 渐进式披露三层（目录/指令/资源），降注意力稀释与误触发 |
| 14 | ASR 选型六组维度；CER 看不到实时性、语义错、交互能力与成本 |
| 15 | 模型按"复杂度×代价×频率"分配；状态机关键是 **failed ≠ uncertain** |
| 16 | 统一 Envelope + 专用解析器 + 置信度一路带下去 |
| 17 | 三层体系 + 五维指标；成功率掩盖过程/体验/安全/成本/证据 |
| 18 | Demo 跑 happy path，生产跑失败路径 |

**口径纪律：永不编 → 口径不升格（应用端是我的，Runtime 底座是团队/开源 Pi）→ ASR/OpenClaw 题标注资料边界 → 边界主动说明，不留暗坑。**
