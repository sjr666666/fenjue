# 01 · 我的画像与简历

> 依据通用模板：`通用焚诀/02-简历与投递/01-简历策略` 与 `02-Bullet生成与四段式写法.md`。
> 简历口径与 `简历/石敬荣_Agent开发_Skill场景版.md`（2026-09-16 最新版）完全一致；投递前按本文件末尾的核对清单过一遍。
> 选点总台见 `16-项目故事与选点台账.md`；历史旧版口径（蓬蓬AI 员工超级助手 / 内容生产端）已停用。

---

## 1. 候选人画像

| 维度 | 事实 |
|---|---|
| 姓名 / 届别 | 石敬荣，2028 届本科（2024.09–2028.06），随时到岗 |
| 学校专业 | 岭南师范学院 · 软件工程；专业前 10%；东软杯三等奖（AI 用药管家，全栈+RAG 知识库问答） |
| 方向 | AI 应用开发 / Agent 开发（主）；AI 应用层工程化、AI 落地/FDE（可投） |
| 技术主线 | Agent 开发与 Agent 工程化：Agent Harness、Tool Calling、MCP、Context Engineering、Agent Eval；多渠道接入、消息可靠性、模型多后端、权限模型 |
| 技术栈 | TypeScript 全栈（React 19 / Zustand / Electron / WebSocket / Hono）、SQLite、Docker、Linux、Git、契约测试 |
| 协作口径 | 两段均为**企业实习交付**：杭州梵域智能 · 八戒农牧**养殖场合规与异常审查 Agent**；深圳辰创科技 · **多渠道获客数字员工平台**。本人为核心开发成员，负责两个 Agent 应用端；执行底座（Pi Runtime 一类可嵌入式 Harness）复用，不升格 |

### 优势（面试重点放大）
1. **真实企业交付背景**：不是练手 demo——梵域实习把 Agent 平台落地农牧企业八戒农牧，做养殖场合规与异常审查 Workflow，交付 72 个工作模板与成果样例；辰创实习建设多渠道获客数字员工，跑通 7 类 IM 的销售跟进链路。
2. **工程化深度**：四账本、串行队列、幂等、fencing、权限硬拦截、Event Trace 回归——这些是大多数应届项目没有的"可靠性 + 可控 + 可回归"维度。
3. **两段经历互补**：横向的**可靠性执行**（多渠道/消息身份/记忆）+ 纵向的**行业深度**（领域知识/合规 SOP/证据与人工审批），合成完整 Agent 应用落地能力。
4. **面试迭代记录**：J 公司/L 公司两场已把雷点补成定稿话术（见 `03`），同类问题不会再被击穿。

### 短板（提前备好话术，见 `04`）
- 无微服务/分布式真实实践；无 Go/Next.js；无 Coze/Dify 直接使用；量化指标待导师给（`00` A2）。
- CS 基础曾翻车（HashMap/二叉树），已补课——凡是简历出现的技术词必须能答基础题。

## 2. 主经历清单（对外讲法）

| # | 名称（简历口径） | 公司/场景 | 角色 | 时间 | 一句话定位 |
|---|---|---|---|---|---|
| 1 | 蓬蓬AI · 养殖场合规与异常审查 Agent | 杭州梵域智能 · 八戒农牧 | 核心开发成员，负责应用端 | 2026.05–2026.08 | 把用药/防疫/消毒/死亡/环境记录串成审查 Workflow，交付 4 类任务 × 6 部门共 72 个工作模板 |
| 2 | 多渠道获客数字员工平台 | 深圳辰创科技 · 企业私域获客 | 核心开发成员，负责应用端 | 2026.02–2026.04 | 数字员工长期驻场 7 类 IM，跑销售 Account Intelligence Workflow，消息不重不丢 |

> 两段是**同一条主线的两次演练**：都是"把 Agent 放进真实业务"，侧重不同——梵域练**行业场景的合规判断**，辰创练**对客执行的可靠性**。不虚构两系统共享代码或上下游衔接（串法见 `16` §3）。

## 3. 定稿简历（当前投递版）

> 原文与 `简历/石敬荣_Agent开发_Skill场景版.md`（2026-09-16 最新）一致；此处为速览 + 标注。**投递前核对清单见 §4。**
> ⚠️ 旧版（`立即到岗` / `立即到岗_优化版` / tinycode 版）口径不同，用哪版投递就按哪版讲。

### 个人摘要（一句话人设）
面向企业业务闭环的 AI Agent 应用开发实习生，具备 Agent Harness、Tool Calling、MCP、Context Engineering 与 Agent Eval 实践。参与八戒农牧企业 AI 助手落地，将养殖知识、业务数据和岗位 SOP 转化为可执行 Agent 工作流，沉淀查、算、写、讲 4 类任务 × 6 类部门共 72 个工作模板。

### 项目一：蓬蓬AI · 养殖场合规与异常审查 Agent（杭州梵域，2026.05–2026.08）
五条 Bullet：
1. **养殖知识与 MCP Tools**：围绕兽药使用、防疫流程、消毒管理和生物安全规范，整理客户制度与现场资料，统一封装知识检索、业务数据读取和成果生成工具；通过 MCP、API、本地数据源接入层完成工具注册、凭据注入与结果回传，使 Agent 按任务调用证据，而非仅依赖 Prompt 内知识。
2. **Agent Harness / 异常审查 SOP**：将审查链路拆为"读取每日记录 → 核对库存与用药 → 检查死亡和环境异常 → 对照防疫规范 → 汇总关联证据 → 输出风险报告"，通过 `BaseAgent` 生命周期完成技能解析、任务上下文注入和工具编排，使不同岗位模板复用同一执行骨架。
3. **Tool Calling / 人工审批**：针对读取经营资料、生成文件及提出处置建议的权限风险，设计 Safe/Ask/Allow-All 三档 Permission Mode，在 `PreToolUse` 阶段集中拦截敏感工具；信息查询和报告草稿可自动执行，正式制度、SOP 与对外材料需人工审核后发布。
4. **多后端与上下文工程**：抽象 `AgentBackend` 和统一 `AgentEvent` 协议，适配 Claude、Pi 两类执行后端；以 Session、Workspace 隔离工作目录、会话状态和工具上下文，动态装配权限规则、客户资料及 `AGENTS.md` 等上下文文件。
5. **Agent Eval / Bad Case 回归**：围绕库存与用药不一致、防疫记录缺失、异常死亡、环境变化未触发提示等失败模式整理测试用例，将文本、工具调用、权限、错误和完成状态记录为可回放 Event Trace，用 Channel Map 契约测试与工具冒烟用例校验流程完整性、定位行为回归。

### 项目二：多渠道获客数字员工平台（深圳辰创，2026.02–2026.04）
五条 Bullet：
1. **销售 Agent Workflow**：把获客流程拆为"汇总客户互动 → 识别 Opportunity → 判断流失或延期风险 → 梳理关键人 → 生成下一步跟进动作"，模型负责语义理解与话术草拟，业务服务负责身份、权限和可靠投递。
2. **多渠道互动归一化**：抽象统一 `IMChannel` 契约和前缀 JID 寻址，把飞书、Telegram、微信、QQ、钉钉、Discord、WhatsApp 的消息转换为内部 Agent 协议，按渠道能力降级流式卡片、消息编辑或普通回复。
3. **可靠性四账本**：基于 SQLite 实现 Inbox、Turn、Outbox、Streaming Card 四类持久化账本，以外部消息唯一键和确定性 `runId` 去重，通过状态迁移、租约 fencing 与 payload hash 保证跟进消息幂等；回执丢失时冻结为 `uncertain` 并转人工裁决。
4. **Account Memory 与安全写入**：将客户事实、销售决策、跟进经验和待办事项沉淀为 Workspace Memory，用 FTS5 检索历史 Timeline，以 revision CAS、幂等键与 HMAC-SHA256 一次性能力签名处理并发写入、防伪造和防重放。
5. **Agent Runtime 与上下文预算**：以 Pi Agent Runner 承载 Agent Loop，通过 stdin/stdout 与文件系统 IPC 管理任务、跟发和中断；将人设与规则拆为 IDENTITY、SOUL、AGENTS、TOOLS 四段 Prompt，配合预检、运行期监控和自动压缩控制长会话上下文。

### 专业技能（五类，检索词对齐）
Agent 开发 / Industry Agent / Agent Eval 与安全 / 工程技术 / AI Coding

## 4. 投递前核对清单（每次投出前过一遍）

- [ ] 电话 1**-****-****、邮箱 ****@qq.com 无误
- [ ] 求职意向与目标岗位名称一致（AI 应用开发 / Agent 开发 / AI 落地实习生）
- [ ] `00` 待确认清单 A1（交付状态）已核实，"上线试运行"口径仍然成立
- [ ] 若该岗位偏好 Mermaid/文档能力 → 补"用 Mermaid 维护架构图"一句（`00` B4）
- [ ] 若该岗位是 FDE/售前向 → 摘要句尾可加一句对齐 JD 的结语（自我介绍模板同款规则）
- [ ] 每条 Bullet 都能被追问 3 层（对应 `03` 拷打树有预案）
- [ ] **确认投递用的哪一版**：最新为 `Skill场景版`；若对方拿的是旧版（立即到岗 / tinycode），自我介绍与选点必须切回对应版本（见 `06` §1）
