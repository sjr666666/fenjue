# 10 · 我的项目协作场景模拟（完整闭环推演）

> 依据通用模板：`通用焚诀/07-项目细节模板/03-实习与经历故事模板.md`（实习故事/Ownership/项目推进/冲突解决）与 `03-项目故事与拷打/02-STAR骨架`。
> ⚠️ 口径声明：本文件是【模拟推演】——角色分工与协作细节基于简历口径（两家企业实习交付、我独立负责两个 Agent 应用端）与项目内参合理补全，**非已核实事实**。`00` 待确认清单 A1/A3 向导师核实后，把本剧本中的设定替换成真实版本。面试中讲协作时，用"核实锚点"标过的细节要谨慎，讲框架（角色边界、我的职责、产出物）是有材料支撑的，可以放心讲。

---

## 0. 角色与全景时间线（模拟设定，A3 核实后校正）

### 团队设定【模拟】

| 角色 | 职责 | 与我的接口 |
|---|---|---|
| 导师 | 项目总负责：客户对接、需求把关、架构评审、里程碑决策 | 给我派活、审我的设计、拦我的风险 |
| 同学甲（平台侧） | 服务端平台能力：账号体系、部署运维、基础监控 | 我的四账本/队列跑在他的 Backend 中枢上 |
| 同学乙（底座集成） | Pi Agent Runtime 集成、容器执行环境 | 我的 Agent 应用跑在他维护的执行引擎上 |
| **我（应用端）** | **两个 Agent 应用端独立负责**：渠道接入/三层身份/四账本应用层/调度/画像 + AgentBackend/AgentEvent/统一 RPC/权限/MCP 接入 | —— |
| 客户方 | 市场部（内容生产端用户）、销售部（承接端用户） | 试用反馈、bad case 来源 |

### 全景时间线（两段经历咬合成闭环）

```text
2026.02 ─── 承接端（辰创）启动：获客流程拆解 → IMChannel 统一渠道契约
2026.03 ─── 三层身份模型 · 四账本/GroupQueue 攻坚 · /pair 绑定机制
2026.04 ─── 会话污染事故排查 · 修复 · 复盘 · 承接端试运行
2026.05 ─── 生产端（梵域 · 八戒农牧）启动：需求调研 → AgentBackend 设计
2026.06 ─── AgentEvent 定稿 · 统一 RPC · 双端联调 · 权限 + 8 大知识库接入
2026.07 ─── 72 个工作模板落地 · 契约测试上线 · 双端试运行
2026.08 ─── 生产端交付：经营材料/物料进入承接端分发，跑通"内容→触达→线索"闭环
```

> 业务闭环一句话：**生产端产出的物料与话术 → 进入承接端分发触达 → 跟进结果沉淀画像 → bad case 回流两端迭代**。协作闭环一句话：**导师给方向和边界，同学给平台和底座，我把 Agent 应用做出来放进真实流程，客户反馈驱动下一轮**。

---

## 1. 阶段一 · 内容生产端（梵域 · 八戒农牧，2026.05–08）

### 场景 1：立项会——"运营的痛点不是缺 AI，是每次换模型都要重做一遍"

**发生了什么**：导师带我跟客户市场部做了两次需求访谈。运营的原话（模拟）："我们多平台发内容，写一篇要两小时；之前试过一个 AI 工具，效果不错，后来要换模型，供应商那边全重做了，我们白等三周。"导师当场把这个转成工程问题，问我："如果模型半年换一次，你的系统应该怎么设计？"

**我的动作与产出**：回来我把这个问题拆成三层——界面不该认识任何模型 SDK（换模型不改页面）、事件必须统一（双端渲染不漂移）、能力必须收口（MCP/API/本地统一接入）。产出了 AgentBackend 抽象的第一版设计稿。

**面试转化**：
- 面试官问法："这个项目的需求是怎么来的？"
- 20 秒答法：「客户市场部的原始痛点是换模型成本——他们之前换一次模型，工具要重做。所以导师定的方向和我做的第一个设计决策就是 AgentBackend 统一模型后端：把 Claude 和 Pi 两类后端的差异收敛成一个流式事件接口，这个约束后来贯穿了整个项目。」

### 场景 2：设计评审会——导师的三个挑战

**发生了什么**（模拟对话）：
> 导师：「为什么不用现成的编排框架，自己抽一层？」
> 我：「框架把模型、编排、UI 绑在一起；我的核心需求是'换模型不动前端'，得先把模型后端这一维独立出来。执行循环我们复用 Pi Agent Runtime，我抽的是应用侧适配层——不是重新造引擎。」
> 导师：「AgentEvent 为什么要做成可辨识联合？直接给字符串不行吗？」
> 我：「双端要复用同一份渲染逻辑，事件类型在类型系统里就得是封闭集合——五种事件穷尽后编译期就查全了，运行时不会出现'这个事件我不认识'。」
> 导师：「权限为什么不信提示词？」
> 我：「提示词是概率约束，模型可以被打动。我们的权限不走提示词，走代码硬控——PermissionManager 是 Claude 和 Pi 两个后端共用的统一权限评估入口（`evaluateToolCall()`），按 safe/ask/allow-all 三档模式裁决工具能不能调；裁决之外还有一条 `runPreToolUseChecks()` 六步管线做二次把关。关键设计是 DANGEROUS_COMMANDS——rm、sudo、chmod、git push 这类命令在 ask 模式下永远弹审批，任何白名单配置都绕不过；安全的白名单（命令/域名 always allow）反过来让常规操作少打断用户。写盘这种敏感操作在 safe 模式是硬编码拦截的，Write/Edit 直接 block。多运营并行时，这个护栏保证任何一条工具调用都在护栏内。」

**我的产出**：评审通过后两周内定稿 AgentBackend + AgentEvent + 权限三件套，成为生产端的骨架。

**面试转化**：这三个问答直接对应拷打树经历二 Q1/Q2/Q3——导师当年问的，就是面试官会问的。

### 场景 3：双端联调——契约测试的由来

**发生了什么**（模拟）：联调第一周，Web 端和 Electron 端对同一个工具事件的处理行为不一致——Web 端能恢复的错误，桌面端直接白屏。排查发现是前端 API、IPC 通道、后端 handler 三处各自演进。同学甲说：「每次都靠人盯，盯不过来的。」

**我的动作与产出**：加了 Channel Map ↔ ElectronAPI 契约一致性测试，校验通道名、参数与返回结构，把漂移拦在开发期——从此双端行为由 CI 守，不靠人盯。

**面试转化**（经历二 Q7 的排查叙事版本）：
- 20 秒答法：「最典型的一次联调问题是双端对同一事件处理不一致。现象是 Web 能恢复的错误桌面端白屏；定位靠事件流记录回放，发现三处各自演进；根因是没有契约约束；修复是统一事件类型加契约测试——这类问题从此在开发期就被 CI 拦住，不再到联调才发现。」

### 场景 4：交付试运行——运营的真实反馈进闭环

**发生了什么**（模拟）：四月生产端进入试运行。市场部反馈最有价值的一条：「生成的物料很好，但发出去之后就没下文了——我们不知道哪条内容带来了客户。」导师把这个反馈带到周会：「这不是内容生产的问题，是承接的问题。」

**我的产出与衔接**：这条反馈直接触发对客承接端立项——生产端物料要有人（数字员工）分发出去并跟进。**这就是两个项目咬合成闭环的由来**，也是我串联故事的真实叙事锚点。

---

## 2. 阶段二 · 对客承接端（2026.04–07）

### 场景 5：获客流程拆解会——"哪些环节敢交给模型？"

**发生了什么**（模拟对话）：销售部访谈后，白板上画了他们的流程：触达→破冰→跟进→画像沉淀。导师问：「这四段里，哪些敢交给模型，哪些打死不能？」
> 我：「语义相关的交模型：破冰话术、意向理解、画像要点抽取。身份、权限、投递可靠性打死不能——模型提议，系统裁决。表格登记线索这种确定性流程，也不要交给模型，直接结构化落库。」

**我的产出**：四段边界 + 「模型只做语义理解与表达」成为承接端的第一设计原则；配套的三层身份模型（Profile/Workspace/Runtime Session）设计稿。

**面试转化**：对应拷打树经历一 Q1/Q2——这段对话就是"为什么 AI 是答案、边界在哪"的真实版本。

### 场景 6：渠道差异争论——同学乙挑战"抹平七渠道"

**发生了什么**（模拟对话）：
> 同学乙：「能不能做一个超集接口，把七渠道功能全包了？」
> 我：「超集等于让每个渠道都假装自己有能力——微信没有的能力调了就报错，等于把坑埋给运行时。我的做法是最小契约加可选能力：约 50 行必须实现的 IMChannel，流式卡片这些差异做成可选方法，微信仅私聊、Discord 流式默认关闭，如实建模。」
> 导师拍板：「分级适配，不强行抹平。」

**面试转化**：对应 M3 与L 公司"渠道对比"题——答「最复杂的是飞书（能力全、流式要求高）」的话术源头就是这场争论。

### 场景 7：四账本设计评审——"为什么不用消息队列？"

**发生了什么**（模拟对话）：
> 同学甲：「消息可靠性，上 RocketMQ/Kafka 不就完了？」
> 我：「渠道消息的可靠性不是投递一次的问题，是'重放多少次都只产生一次效果'的问题。我的幂等键天然长在业务身份上——inbox 按渠道消息 ID 全局去重、turn 以逻辑键哈希做确定性身份、outbox 按轮次加序号幂等。SQLite 四张账本把这些语义直接落库，跟调度同事务；上外部队列反而要维护两套状态源。单机场景，SQLite 是唯一可信状态源。」
> 导师：「那 sending 之后超时呢？」
> 我：「不重试。Provider 可能已经收到了，副作用不可知——fence 成 uncertain 冻结整个 Turn，人工裁决。宁可冻结，不可重复。」

**我的产出**：四账本 + 三句哲学（先落库再动手/不确定即冻结/恢复尊重租约）定稿，后续经历 v60 迁移与故障注入测试验证。

**面试转化**：拷打树经历一 Q5/Q6 的直接来源——面试官问"为什么不用 MQ"就用同学甲当年的问题来答。

### 场景 8：会话污染事故——试运行期间最严重的一次（真实事件，源自L 公司面经）

**发生了什么**：销售反馈客户在会话 A 里收到了会话 B 的回复内容。当天定位：
1. 复现——固定输入重放，稳定出现；
2. 看 Trace——把各会话的写入记录拉出来，发现多个智能体执行完都会把结果写回公共上下文，没有按 session 隔离；
3. 根因——共享存储无隔离边界；
4. 修复——三层模型强化隔离 + 串行队列保证同会话不并发 + 双 revision 乐观锁防覆盖。

**协作细节（模拟）**：复盘会上导师定的规矩：「这类事故以后不许口头归档，全部进 bad case 清单。」同学甲补了监控告警，我补了写入隔离的回归测试。

**面试转化**（L 公司定稿 STAR 版）：现象→复现→Trace→根因→修复，完整可背诵版本见 `03` 经历二 Q7 同构题与L 公司复盘 §7。

### 场景 9：绑定机制的现场考验——"这个客户到底归谁"

**发生了什么**（模拟）：试运行第二周，销售群里出现归属争议：同一个客户先在微信私聊过，后来进了销售群。我按粘性归属（resolveStickyChannelOwner）处理——会话粒度的归属判定，不允许第一条消息的话题 ID 永久锁死回复锚点。导师追问边界：「如果两个销售都声称这客户是自己的呢？」我：「这正是 owner_claim_source 信任阶梯存在的意义——绑定来源分五级信任，configured 最高，auto 最低；争议走后台人工确认，不靠模型猜。」

**面试转化**：OneID 雷点（`03` L1）的场景化版本——被问"怎么知道是同一个人/归谁"时，讲这个真实场景比背概念有力得多。

---

## 3. 阶段三 · 闭环运行与交付（贯穿 2026.05–07）

### 场景 10：uncertain 裁决的日常运营

**发生了什么**（模拟）：试运行期间每周的例行动作——打开 uncertain 列表（回执丢失被冻结的 Turn），人工裁决：确认送达的标记 delivered，确认失败的释放重发。导师要求：「裁决记录留痕，月度复盘看 uncertain 率有没有恶化。」

**面试转化**：「你说的人工裁决具体是什么？」→ 「每周固定看 uncertain 列表：已送达的标 delivered，失败的释放重发，全部留痕。这是消息可靠性'最后一公里'的人工兜底，也是 bad case 回流的入口之一。」

### 场景 11：bad case 双端回流迭代

**发生了什么**（模拟）：客户反馈"数字员工回复太啰嗦"→ 承接端定位到人设 IDENTITY 段描述过重 → 调整后下发；同一周期，生产端根据内容使用数据调整了物料模板结构。**闭环的最后一环**：两端各自迭代，但回流机制统一（bad case 清单 → 周会定优先级 → 修改 → 回归测试 → 下发）。

**面试转化**：「你们项目上线后怎么迭代的？」→ 「两条回流线：可靠性侧每周 uncertain 裁决，效果侧 bad case 清单驱动人设与模板迭代。我的角色是把回流机制建起来，保证迭代不是拍脑袋。」

### 场景 12：交付验收（闭环终点）

**发生了什么**（模拟）：七月交付演示，跑的是完整闭环链路——运营在生产端出物料 → 物料进承接端 → 数字员工在客户私聊触达 → 跟进记录沉淀画像 → 人设迭代回流。导师验收口径（模拟，A1 核实锚点）：功能闭环跑通 + 消息可靠性语义验证（不重不丢）+ 试用方日常使用。

**面试转化**：「项目最后怎么验收的？」→ 「按闭环演示验收：内容生产到触达跟进沉淀全链路走通，加上消息投递的可靠性语义验证。规模类数字我手头没有第一手口径，可以会后跟您对齐我们的度量方式。」（`00` A1/A2 核实后替换成真实数字）

---

## 4. 面试官视角 · 协作类问题应答卡

| 面试官问 | 20 秒答法（基于本剧本 + 真实材料口径） |
|---|---|
| 团队几个人？怎么分工？ | 「导师带队管客户对接和架构评审，平台侧和底座集成各有一位同学，我独立负责两个 Agent 应用端——渠道接入、身份模型、可靠性、调度、画像这块。」【核实锚点 A3】 |
| 你和导师怎么协作？ | 「他给方向、定边界、做评审——我的每个关键设计（AgentBackend、三层模型、四账本）都过他的评审会，他会挑战'为什么不用现成的/为什么不用消息队列'，很多设计是被他问出来的。」 |
| 你和同学怎么协作？ | 「我的应用跑在同学维护的平台和执行底座上——接口是他们对我的约束，也是我给他们的需求；联调出问题一起排查，比如双端漂移那次的契约测试就是联调逼出来的。」 |
| 项目里你主导了什么？ | 「两个应用端从设计到实现到我兜底：四段边界怎么拆、四账本怎么设计、分级适配还是强行抹平，这些技术决策是我提出、评审通过的——'主导'限于我负责的两端，平台和底座是同学和导师的。」（口径不升格） |
| 遇到分歧怎么解决？ | 「拿场景和约束说话：渠道差异那场争论，我用'微信没有的能力调了就报错'这个场景说服了超集方案的风险；四账本那场，我用幂等键长在业务身份上这个论证回应了'为什么不直接上 MQ'。」 |
| 项目最大的收获？ | 「学会了在真实业务里收模型的边界——模型提议、系统裁决这条原则，是从导师的评审、客户的事故、和同学的争论里一次次被验证出来的。」 |

## 5. 待核实锚点（模拟 → 真实的校正清单）

| 剧本设定 | 需核实什么 | 核实后动作 |
|---|---|---|
| 团队 = 导师 + 2 同学 + 我 | 实际人数与分工 | 替换 §0 团队表；更新 `08` 归因 |
| 场景 4 的"物料无下文"触发承接端立项 | 两个项目的真实衔接缘由 | 校正串联故事讲法 |
| 场景 7 的评审对话细节 | 四账本是否真经过评审会、谁挑战的 | 校正 M5 叙事背景 |
| 场景 10/11 的周度运营节奏 | 试运行期的真实迭代节奏 | 校正 A5；补 bad case 实例 |
| 场景 12 的验收口径 | 交付标准与当前状态 | 替换 `00` A1；回填 `09` 数字锚点 |
| 导师评审会的存在与频率 | 是否有正式评审环节 | 无则降级为"导师 code review / 周会把关"口径 |

> 使用纪律：本剧本解决的是"协作叙事的自洽性"，让 A1/A3 的核实问题有了具体的问法（拿 §5 去问导师就是现成的提纲）。核实后把模拟设定替换成事实，剧本即升级为真实复盘；核实不到的细节，面试中按应答卡的框架讲角色边界与产出物（有材料支撑），不引用未核实的对话细节。
