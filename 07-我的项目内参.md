# 07 · 我的项目内参

> 依据通用模板：`通用焚诀/07-项目细节模板/02-经历内参Skill模板.md`（R 点体系：能力名 + 边界 + 证据 + 答法 + 风险等级 + 降级链）。
> 证据索引指向本地内参库：`项目/miniclaw付费版内参/`、`项目/craft-agents付费版内参/`（含 R01–R27 全量要点、Q&A 与源码 wiki）。深挖某个点时去对应内查阅，本文件是作战速查层。
> 纪律：R 点只声明 Scope Boundary 内的能力；讲不清沿降级链走，不硬撑。

---

# 经历一 · 多渠道获客数字员工平台（8 个主力 R 点）

## M1 三层身份模型与 identity_hash（对应内参 R02）
- 一句话：身份（Profile）、边界（Workspace）、记录（Runtime Session）分离，身份变更可审计传播。
- 边界：我设计并实现三层模型与 identity_hash；不是数据库运维方案。
- 细节弹药：四张核心表；identity_hash 由四段提示词+运行策略+名称共同计算、配递增 version；Session 刻意不叫 sessions（防被当成第二个产品级 Agent）；folder 名正则约束防路径穿越。
- 风险：中。降级链：答不上 → 降到 M7（四段人设）讲设计层。
- 关联拷打：`03` Q4/L4。

## M2 四层职责边界架构（对应内参 R03）
- 一句话：Backend 唯一有状态中枢 / Pi Runner 无库无端口只做执行 / Workspace 文件真相锚点 / 客户端零令牌。
- 边界：执行引擎（Pi Runtime）是团队复用底座；四层切分的应用侧设计是我的。
- 细节弹药：Runner 走 stdin/stdout/IPC 目录三条通道而非网络 API——部署简单、执行载体可透明替换；Docker 只隔离执行环境不替代授权层。
- 风险：低（设计层即可讲）。降级链：无。

## M3 七渠道 IMChannel 统一契约（对应内参 R07）
- 一句话：约 50 行最小契约（connect/disconnect/sendMessage/setTyping/isConnected）+ 前缀 JID 寻址，能力差异做成可选方法分级适配。
- 边界：**不能说"七渠道功能完全一致"**——微信仅 P2P、WhatsApp 个人号逆向协议有封号风险、QQ 流式仅 C2C、Discord 流式默认关闭。
- 细节弹药：新增渠道只需实现 IMChannel 控制器；Web 天然是第八个渠道；流式卡片三级降级（70ms/字符打字机 → CardKit 全量替换 ≥1000ms → 传统 patch）+ LRU 去重缓存 1000 条/30 分钟。
- 风险：中。降级链：答不上 → 讲"统一的是不变链路，可选的是差异能力"。

## M4 多账号绑定与信任阶梯（对应内参 R08）★OneID 雷点正解
- 一句话：账号作用域 JID 地址代数 + 三层绑定模型（channel_accounts → registered_groups → channel_mounts）+ `/pair` 配对码五重校验 + `owner_claim_source` 五级信任阶梯。
- 边界：绑定机制是我实现的应用侧能力；平台级 unionid 类开放能力按渠道实际支持讲。
- 细节弹药：入站回调先过 `inboundAllowed()` 门控再 `scopeChannelJid`；粘性归属（resolveStickyChannelOwner）防第一条消息话题 ID 永久锁死锚点；凭证转移做信任隔离（白名单清空、受众收紧 owner_only）。
- 风险：中高（需源码级理解——已核实过）。降级链：答不上 → 用J 公司定稿概念版（OneID 显式绑定四路径：unionid/手机号/扫码/人工）。

## M5 可靠性四账本状态机（对应内参 R09）★核心王牌
- 一句话：Inbox 全局去重 / turn_runs 以 runId 确定性身份 / Outbox 幂等键分阶段投递 / StreamingCard revision CAS 单活跃卡。
- 细节弹药（去重键必背）：inbox=UNIQUE(provider, account_id, external_message_id)；turn_runs=runId sha256(logicalKey) 创建即去重；outbox=UNIQUE(turn_run_id, ordinal)+payload_hash，pending→claimed→uploading→uploaded→sending→delivered；sending 后模糊超时 fence 为 uncertain 冻结整个 Turn 转人工。
- 三句哲学：先落库再动手 / 不确定即冻结 / 恢复尊重租约。
- 风险：高（面试官最爱挖）。降级链：uncertain 讲不清 → 退到"至少一次 + 副作用安全"的语义层。

## M6 GroupQueue 串行调度（对应内参 R10）★L 公司雷点正解
- 一句话：JID 按 `{folder}#{agentId}` 划串行车道，四道入队闸门 + 指数退避 + 双时钟 + 分级卡死恢复。
- 细节弹药：四道闸门=终态丢弃/变更暂停 MutationPauseToken/活跃检查写 _drain 哨兵/容量检查；指数退避 MAX_RETRIES=5、BASE_RETRY_MS=5000；双时钟=活动时钟+IPC 债务时钟；分级终止=_close 2s→docker stop 15s→强杀；用户中断保留退避进度防死循环。
- 风险：中高。降级链：细节记不清 → 退到L 公司定稿骨架（入口→哈希路由→串行队列→Worker 池上限→退避→死信）。

## M7 四段 Prompt 与上下文预算（对应内参 R15）
- 一句话：人设拆 IDENTITY/SOUL/AGENTS/TOOLS 四段，配三层上下文预算——团队互不越界、人设可控、长会话成本可控。
- 风险：中。降级链：降到"结构化提示词 + 预算控制"设计层。

## M8 ACL 权限矩阵与 Owner 门控（对应内参 R12）
- 一句话：系统能力与资源归属双维度授权；admin 捷径仅限系统级，工作区层的 canAccess/canModify/canDelete 不读 role 字段——无旁路。
- 细节弹药：八层权限栈覆盖 REST/WebSocket/IM/MCP-IPC 四入口同源裁决；资源级 404 反枚举；七个变更类 IM 命令强制 owner 判定。
- 风险：中高。降级链：退到"模型提议、系统裁决"原则 + PermissionManager 概念。

---

# 经历一 · 深挖备弹区（M9–M14，被加时赛追问时启用）

> 口径纪律：M9–M14 多属【项目整体架构】——讲整体设计 + 我的关联点；M1–M8 是简历 Bullet 明写的【本人实际参与】主力。

## M9 双执行模式与暖 Runner（对应内参 R06）
- 一句话：Container 隔离执行 / Host 活体特权双模式，三层裁决链 + 派生超时公式排除竞态。
- 细节弹药：Host spawn 前重查 admin+active（"持久化工作区不是授权授予"）；idleCloseMs=min(executionTimeout, idleTimeout)、watchdogMs=max(...)+15s——公式源自真实事故（两默认值同为 30 分钟时 watchdog 确定性误杀）；暖 Runner 不关进程直接 IPC 注入跳冷启动；exit code 137 连续两次清会话破 OOM 死循环。
- 风险：中高。降级链：公式记不全 → 只说"watchdog 恒晚于优雅回收"这层结论。

## M10 三条零网络进程通道（对应内参 R05）
- 一句话：stdin 引导 + stdout 分帧 + 文件系统 IPC 目录，替代网络 API。
- 细节弹药：stdout 分帧用括号深度匹配不用字符串定界（防回复文本含字面量标记欺骗）；fs.watch 50ms 防抖 + 5000ms 后备轮询防 inotify 丢失；MCP 外呼"写请求文件+轮询结果文件"，wx flag + 0600 + 只吞 ENOENT 修 TOCTOU。
- 误区防守：选型理由是持久性与跨容器可见性，**不是性能**。
- 风险：中高。

## M11 启动恢复与五阶段停机（对应内参 R04）
- 一句话：先恢复状态 → 再开端口 → 最后连渠道；双游标不互合成，at-least-once 重放优于丢失。
- 细节弹药：四连对账（Inbox/Outbox/流式卡/IPC）；五阶段停机顺序是修 bug 的产物。
- 风险：中高。降级链：退到"SQLite 唯一可信状态源 + 宁可重放不可丢失"。

## M12 Workspace Memory v2（对应内参 R17）
- 一句话：双 revision CAS 防丢更新 + HMAC 能力签名一次性消费防伪造 + 四类知识结构化存储。
- 细节弹药：fact/decision/lesson/open_loop；快照声明 trust="historical_data" 防提示注入；FTS5 关键词检索（无向量——不暗示 RAG）；tombstone 软删除保留"谁在何时为何忘记"审计链。
- 风险：中。

## M13 定时任务 V2 物化（对应内参 R14）
- 一句话：游标兼任乐观锁 + 单事务三件事（锁行/插唯一行/推游标）消灭双调度、死循环、编辑竞态。
- 细节弹药：周期任务记 missed 不补跑、一次性任务必达（显式承诺）；租约 fencing 防过期写入；慢任务"跳过+可审计"不排队雪崩。
- 风险：中高。降级链：退到"at-least-once 投递 + at-most-once 执行"语义层。

## M14 命令系统与 Owner 门控（对应内参 R11）
- 一句话：七个变更类命令强制 senderImId===owner_im_id 的纯数据集合 + 命令检查点四状态防副作用重复执行。
- 细节弹药：内置命令管线前短路永不到达 Agent；两条不变量（释放必须降级 activation_mode 否则 Bot 永久沉默；认领绝不触碰激活策略）；/clear IM 走 Owner 门控 Web 走 ACL 的"语义统一鉴权分治"。
- 风险：中。

> 更深弹药（讲到即取）：沙箱三防御轴（R13）、Schema 三层迁移与 VACUUM 备份门控（R19）、HMAC 签名 Cookie 与 CSWSH（R20/R21）、Skills 六层优先级与清单指纹（R16）、用量计费半小时桶（R18）、StreamEvent 三端同步四道防线（R23）——均在 `项目/miniclaw付费版内参/03-Q&A`，讲不稳的按各条"降级说法"执行。

---

# 经历二 · AI 营销内容生产助手（6 个主力 R 点）

## C1 AgentBackend 统一模型后端（对应内参 R03/R05）
- 一句话：ClaudeAgent/PiAgent 收敛为 provider-agnostic 流式事件接口，换模型不改前端。
- 边界：我做的是应用侧多后端适配层；不是自研模型编排引擎（执行循环复用 Pi Runtime）。
- 细节弹药：差异收敛在三处——事件模型、子进程 JSONL 通信、工具代理；Thinking Level 与模型配置随抽象流转（capabilities-driven UI）。
- 风险：中。降级链：退到"适配器模式 + 统一事件契约"设计层。

## C2 AgentEvent 事件流（对应内参 R10）
- 一句话：可辨识联合类型统一五种事件（文本增量/工具启停/权限请求/错误/完成），双端渲染与错误恢复行为一致。
- 细节弹药：类型系统封闭集合，渲染层 switch 穷尽；中间态（工具进行中/权限交互/错误恢复）都能表达。
- 风险：中。降级链：退到"事件驱动 vs 字符串返回"的好处。

## C3 PermissionManager 权限体系（对应内参 R06；源码：`shared/src/agent/core/permission-manager.ts` + `pre-tool-use.ts`）
- 一句话：Claude/Pi 双后端共用的权限硬控——`evaluateToolCall()` 按 safe/ask/allow-all 三档裁决 + `runPreToolUseChecks()` 六步管线二次把关，提示词只是软约束。
- 细节弹药：DANGEROUS_COMMANDS（rm/sudo/chmod/dd/kill/curl/git push 等）ask 模式强制审批、白名单绕不过；命令/域名 always allow 会话级白名单；safe 模式硬编码 block Write/Edit/MultiEdit/NotebookEdit；管线六步=模式检查→源阻断→前置条件→call_llm 拦截→输入转换（路径展开/配置写前校验/技能限定/metadata 剥离）→ask 提示决策。
- 误区防守：「权限=提示词约束」是错的——提示词可被说服，DANGEROUS_COMMANDS 与硬编码 block 不可绕过。
- 风险：中高（安全题高频）。降级链：退到金句"模型提议、系统裁决" + safe/ask/allow-all 概念层。

## C4 IPC/WebSocket 统一 RPC（对应内参 R12）
- 一句话：本地（Electron IPC）与远程（WebSocket）双传输复用同一套会话、文件与模型连接逻辑。
- 风险：低。迁移证据：与经历一 IMChannel 同构——"统一的都是变化的维度"。

## C5 契约一致性测试（对应内参 R13）
- 一句话：校验 Channel Map ↔ ElectronAPI 通道名、参数与返回结构，多端接口漂移拦在开发期。
- 细节弹药：IPC 特别容易漂移，因为前端 API、IPC 通道、后端 handler 三处各自演进。
- 风险：低。

## C6 Session/Workspace 隔离与持久化（对应内参 R08/R09）
- 一句话：会话生命周期、工作目录、偏好与工具上下文以 Session/Workspace 为边界隔离；运行时 Message 与持久化 StoredMessage 分治。
- 迁移证据：三层隔离思想在经历一升级为 Profile/Workspace/Runtime Session。
- 风险：低。

## C7 BaseAgent 模板方法生命周期（对应内参 R04）
- 一句话：技能解析、前置条件、分支上下文注入、消息重写是全后端共享的 turn 前置，放基类；模型调用留给子类。
- 误区防守：是模板方法不是普通继承——差异点在"谁执行模型调用"。
- 风险：中。降级链：退到"共享前置 + 子类执行点"。

## C8 JSONL 会话持久化（对应内参 R09）
- 一句话：流式长任务的会话按行追加、可恢复；运行时态（isStreaming）不落盘。
- 误区防守：JSONL 不是"图简单"——是流式多事件任务的形态决定的。
- 风险：中。

## C9 System Prompt 动态构建（对应内参 R11）
- 一句话：权限模式、环境元数据、用户偏好、AGENTS.md/CLAUDE.md 上下文发现按工作区动态装配，缓存与大小限制控膨胀。
- 迁移证据：与经历一四段 Prompt（IDENTITY/SOUL/AGENTS/TOOLS + 20000 字符上限）同一条"结构化人设"主线。
- 风险：中。

## C10 OAuth 凭据与 Token Refresh（对应内参 R19）
- 一句话：Source 提供外部系统连接配置与凭据，Skill 提供使用逻辑，ServerBuilder 转成可调用工具；refresh 带冷却时间防请求风暴。
- 误区防守：Source ≠ Skill——一个管连接与凭据，一个管逻辑与模式。
- 风险：中。

---

# 权限哲学对照（源码核实 · 面试必考对比题）

> 两个项目的权限设计截然不同，被问"两个项目权限怎么做的"时按此答，别混。

| 维度 | 经历二 · craft-agents（内容生产端） | 经历一 · miniclaw（承接端） |
|---|---|---|
| 工具执行权限 | PermissionManager 硬控：`evaluateToolCall()` safe/ask/allow-all 三档 + `runPreToolUseChecks()` 六步管线；DANGEROUS_COMMANDS 强制审批 | **刻意保持开放**（CLAUDE.md 原文"不要虚构只读或受限工具模式"）——不放工具护栏 |
| 权限收口在哪 | 工具调用本身（执行前拦截） | 资源层（ACL 矩阵/挂载白名单）+ 命令层（OWNER_REQUIRED_IM_COMMANDS 七命令强制 owner）+ 渠道层（准入/群归属） |
| 代表性机制 | DANGEROUS_COMMANDS（rm/sudo/git push 永远审批）、always allow 白名单、mcp/api mutation 提示 | `/pair` 绑定 + owner_claim_source 五级信任阶梯、canAccess/canModify/canDelete 无 admin 旁路、404 反枚举 |
| 面试讲法 | 「权限拦在工具执行的门口」 | 「工具放开做，边界收在三层——资源归属、命令权限、渠道准入」 |

> 一句话区分：**生产端防的是"AI 乱动"，承接端防的是"人/渠道越权"**——两端防的根本不是同一个东西，分开讲反而显得你懂边界。

---

## R 点组合方案（按面试类型选）

| 面试类型 | 首发组合 | 备选 |
|---|---|---|
| AI 应用开发（工程向） | M5 四账本 + M6 调度 + C1 模型后端 + C3 权限 | M1、C5 |
| FDE/AI 落地（业务向） | M3 渠道适配 + M7 人设 + C1 + 串联故事 | M2、C6 |
| 深挖安全 | C3 + M8 + M4 信任阶梯 | 容器沙箱三防御轴（内参 R13） |

> 未收录的高风险深挖点（备用）：容器沙箱三防御轴、定时任务 V2 物化、stdin/stdout 分帧协议——在 `项目/miniclaw付费版内参/02-简历要点库` R05/R06/R13/R14，被深挖时再取。
