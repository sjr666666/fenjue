# 16 · 项目故事与选点台账

> 上游（只读）：`简历/小石_Agent开发_项目故事与选点对照.md`（完整故事与源码级讲解）+ `简历/小石_Agent开发_Skill场景版.md`（最新投递简历，口径唯一来源）。
> 下游：`02`（三档口述）｜`03`（拷打树）｜`07`（R 点内参）｜`09`（一页纸）。
> **本文件是"选点总台"**：把两个项目的故事、可用选点、含金量、追问准备度、当前在用状态集中在一处；换简历版本时先改这里，再往下游同步。

---

## 0. 先说结论：2026-09-16 口径已切换

最新简历（Skill 场景版）与旧焚诀有一个**主线级差异**，必须最先记住：

| | 旧口径（2026-09-13 之前） | **新口径（现用）** |
|---|---|---|
| 两段实习的关系 | "同一获客闭环的两个端"：内容生产端 → 多渠道触达 → 线索沉淀 | **同一条主线的两次演练**：行业场景的合规判断（梵域·养殖） + 多渠道对客的可靠性执行（辰创·销售） |
| 梵域定位 | 蓬蓬AI · 员工超级助手（内容生产端） | **蓬蓬AI · 养殖场合规与异常审查 Agent** |
| 梵域叙事核心 | 多端多模型统一 / 换模型不改前端 | **养殖合规与异常审查 Workflow**（读数→核对→识别异常→对照规范→汇总证据→出报告），统一 Harness 与 Eval 兜底 |
| 辰创叙事核心 | 7 渠道接入 + 三层身份 + 四账本 | **销售 Account Intelligence Workflow** + 多渠道互动归一化 + 四账本可靠投递 + Account Memory |
| 数字锚点 | 72 模板 + 8 大知识库（员工超级助手口径） | **查/算/写/讲 4 类任务 × 6 类部门 = 72 个工作模板**；养殖/生物安全合规成果样例 |

> ⚠️ **红线**：以后**不再讲"内容生产端 ↔ 获客承接端"的闭环两端叙事**。两段实习是**各自独立的企业交付**，用"主线一致（把 Agent 放进真实业务）+ 侧重点不同（合规判断 / 可靠性执行）"来串。旧叙事留在 `简历/小石_Agent开发_项目故事与选点对照.md` 里做历史对照，不进新面试口径。

---

## 1. 梵域 · 蓬蓬AI 养殖场合规与异常审查 Agent

### 1.1 一句话定位

> 把养殖场的用药、防疫、消毒、死亡与环境记录，从"分散在客户制度与现场资料里、靠人工交叉核查"变成 **Agent 可按 SOP 调证据、出风险报告** 的合规审查工作流；同一套 Harness 复用到经营、财务、公关、商贸、生产等岗位。

### 1.2 项目事实（只填事实，不编）

```text
公司/时间：杭州梵域智能科技有限公司 · AI 应用开发实习生 · 2026.05–2026.08
客户/场景：八戒农牧（农牧企业）· 养殖场合规与异常审查
业务痛点：用药、防疫、消毒、死亡、环境记录分散；现场问题依赖人工交叉核查；
          不同模型后端与桌面/Web 端行为易漂移；敏感操作缺硬边界；Agent 行为缺可回归的验证
项目目标：把高频岗位工作转成可复用的 Agent 工作模板；让模型能按任务调用证据，
          同时保证敏感操作可控、会话互不污染、行为可复现可回归
系统输入：客户制度、养殖知识、业务数据、每日记录
核心链路：读取每日记录 → 核对库存与用药 → 检查死亡与环境异常 → 对照防疫规范
          → 汇总关联证据 → 输出风险报告
我的实际职责：核心开发成员，负责本 Agent 应用端（Harness 生命周期 / 工具接入层 /
              权限拦截 / 多后端适配 / 上下文装配 / Eval 回归）
协作边界：执行底座（Pi / Claude Agent SDK 一类可嵌入式 Harness）复用；我做的是应用端工程化
已知结果：交付查/算/写/讲 4 类任务 × 6 类部门共 72 个工作模板；
          形成生物安全、成本分析、生产复盘、政府申报等成果样例；完成客户资料与部署交接
边界：Agent 出风险报告与正式材料初稿，不替代兽医/管理者做最终判断；
      正式制度、SOP 与对外材料需人工审核
待确认：交付状态（上线试运行 vs 开发收尾）、使用规模（见 `00` A1/A2）
```

### 1.3 选点台账（梵域）

> 选点 = 简历上用来体现这个项目技术含金量的切入点。含金量依据 = 为什么这个点值钱；追问准备度 = 现在能不能扛住 3 层追问。

| 编号 | 选点 | 含金量依据 | 追问准备度 | 当前在用 |
|---|---|---|---|---|
| B1 | **Agent Harness / 异常审查 SOP** | 把 SOP 映射成"每步有输入/工具/输出/失败处理"的可执行可观察步骤；`BaseAgent` 统一技能解析、前置条件、上下文注入、消息重写 | ⚠ 需补读源码（`BaseAgent` 生命周期细节） | ✅ 在用（简历 b2） |
| B2 | **Tool Calling / 人工审批** | 软约束 + 硬拦截两层：Permission Mode 进系统提示词，`PreToolUse` 在每次调用前集中检查；"模型提议、系统裁决" | ✅ 够（`07` C3 + 内参 R13） | ✅ 在用（简历 b3） |
| B3 | **养殖知识与 MCP Tools** | 制度/养殖知识/业务数据统一成工具契约，而非塞进 Prompt；知识检索、数据读取、计算分析、成果生成按任务调用 | ⚠ 需补读工具注册与凭据注入实现 | ✅ 在用（简历 b1） |
| B4 | **多后端与上下文工程** | `AgentBackend` 收敛 Claude/Pi 差异 + 可辨识联合 `AgentEvent`；Session/Workspace 隔离 + 上下文文件发现范围/大小/缓存约束 | ✅ 够（`07` C1/C4/C9） | ✅ 在用（简历 b4） |
| B5 | **Agent Eval / Bad Case 回归** | 事件 Trace 可回放 + 契约测试 + 冒烟用例，覆盖"库存对不上/防疫缺失/异常死亡/环境恶化未提示"等失败模式 | ⚠ 需把 4 个 Bad Case 各讲成一个具体例子 | ✅ 在用（简历 b5） |

**当前首发组合（AI 应用/Agent 开发向）**：B2 权限 + B1 SOP + B5 Eval（工程含金量最高）
**FDE/落地向首发**：B3 养殖知识与 MCP + B1 SOP + 72 模板成果

---

## 2. 辰创 · 多渠道获客数字员工平台

### 2.1 一句话定位

> 让 AI 数字员工长期驻场 7 类 IM，把"客户互动 → 机会判断 → 风险识别 → 关键人梳理 → 下一步跟进 → 画像沉淀"变成可持续运行、消息不重不丢、记忆不被伪造的销售 Agent Workflow。

### 2.2 项目事实（只填事实，不编）

```text
公司/时间：深圳辰创科技有限公司 · AI 应用开发实习生 · 2026.02–2026.04
业务用户：客户方市场与销售部门（销售在 IM 里用数字员工承接客户）
业务痛点：客户互动分散在飞书/微信/社群；销售难连续掌握机会状态与下一步动作；
          回执可能丢失、消息可能重复、多 Runner 并发、跨会话数据污染
项目目标：把销售跟进变成可持续运行的 Agent Workflow；消息投递、执行状态与记忆写入
          具备幂等、可恢复、可审计特性
系统输入：7 类 IM 消息（飞书/Telegram/微信/QQ/钉钉/Discord/WhatsApp + Web）
核心链路：消息入站 → 幂等去重 → 身份与权限 → 串行队列 → Agent 执行 → 权限矩阵 → 回复/流式卡片
我的实际职责：核心开发成员，负责 Agent 应用端（渠道接入/身份模型/四账本可靠性/调度/画像记忆）
协作边界：执行引擎（Pi Agent Runtime）是复用底座；四层切分的应用侧设计是我的
技术与事实清单：
  - 销售 Workflow：Account → 最近互动 → 业务变化 → Opportunity → Risk → Stakeholder → Next Best Action
  - IMChannel 统一契约（约 50 行最小集合 + 可选能力方法）+ 前缀 JID 寻址
  - 四账本：Inbox（UNIQUE 去重）/ Turn（runId 确定性身份）/ Outbox（幂等键 + 分阶段状态机）/
    StreamingCard（revision CAS）
  - GroupQueue 串行队列：四道入队闸门、指数退避、双时钟、卡死恢复
  - Workspace Memory：事实/决策/经验/待办四类知识 + FTS5 + revision CAS + HMAC 一次性签名
已知风险/异常/兜底：sending 后模糊超时 fence 为 uncertain 冻结转人工；
  WhatsApp 个人号逆向协议有封号风险；微信仅 P2P
已知结果：形成可跨渠道复用的获客数字员工执行链路（规模数据【待确认，见 00-A1/A2】）
```

### 2.3 选点台账（辰创）

| 编号 | 选点 | 含金量依据 | 追问准备度 | 当前在用 |
|---|---|---|---|---|
| A1 | **可靠性四账本与 uncertain** ★王牌 | Inbox 全局去重 / Turn 确定性 runId / Outbox 分阶段幂等 / StreamingCard revision CAS；"先落库再动手、不确定即冻结、恢复尊重租约" | ✅ 够（wiki 已补到 ✅，内参 R09） | ✅ 在用（简历 b3） |
| A2 | **销售 Account Intelligence Workflow** | 把销售 SOP 映射成 Timeline → Opportunity/Risk/Stakeholder → Next Best Action；模型给判断与草稿，服务端守身份/权限/副作用 | ⚠ 需把 Opportunity 判据讲成 1 个具体客户例子 | ✅ 在用（简历 b1） |
| A3 | **多渠道互动归一化** | 约 50 行 IMChannel 最小契约 + 前缀 JID；能力差异做成可选方法分级适配（不强行抹平） | ✅ 够（内参 R07） | ✅ 在用（简历 b2） |
| A4 | **Account Memory 与安全写入** | 四类知识 + FTS5 检索历史 Timeline；revision CAS 防并发覆盖、幂等键防重复、HMAC-SHA256 一次性签名防伪造防重放 | ✅ 够（内参 R08/R09） | ✅ 在用（简历 b4） |
| A5 | **Agent Runtime 与上下文预算** | Pi Runner 承载 Agent Loop，stdin/stdout + 文件系统 IPC 管理任务/跟发/中断；会话串行队列；四段 Prompt（IDENTITY/SOUL/AGENTS/TOOLS）+ 预检/监控/自动压缩 | ⚠ 需补读分帧协议细节 | ✅ 在用（简历 b5） |
| A6 | 三层身份模型与 identity_hash | Profile/Workspace/Runtime Session 分离，身份变更可审计传播 | ⚠ 中（主动讲并发写边界） | ⬜ 备用 |
| A7 | 多账号绑定与信任阶梯（OneID） | `/pair` 配对码五重校验 + `owner_claim_source` 五级信任阶梯 + 粘性归属 | ✅ 够（J 公司雷点正解） | ⬜ 备用 |
| A8 | GroupQueue 串行调度 | 四道入队闸门 + 指数退避 + 双时钟 + 分级卡死恢复 | ✅ 够（L 公司雷点正解） | ⬜ 备用 |

---

## 3. 两段实习怎么串（新串法，背这段）

> 我这两段企业实习，是同一条主线的两次演练——**把 Agent 放进真实业务流程，解决它怎么接入、怎么执行、怎么控风险、怎么稳定运行**。
> 深圳辰创那段做的是**对客执行的可靠性**：数字员工驻场 7 类 IM，我做渠道归一、身份与权限、四账本可靠投递、会话串行调度和画像记忆——核心是"多人、多渠道怎么不重不丢、不越权"。
> 杭州梵域那段做的是**行业场景的合规判断**：给八戒农牧做养殖场合规与异常审查 Agent，把用药、防疫、消毒、死亡、环境记录串成"读数→核对→识别异常→对照规范→汇总证据→出报告"的审查 Workflow，同一套 Harness 复用到 6 类部门 72 个岗位模板——核心是"行业 SOP 怎么变成可执行、可观察、可回归的 Agent 能力"。
> 串起来看，一次练的是**横向的可靠性**（多渠道、消息、身份、记忆），一次练的是**纵向的行业深度**（领域知识、合规 SOP、证据与人工审批）。合起来正好是"Agent 能不能真正落地到真实业务"要同时具备的两面。

> ⚠️ 两段是**各自独立的企业交付**，不虚构共享数据库或上下游衔接。

---

## 4. 选点组合方案（按岗位类型）

| 面试类型 | 首发组合 | 备选 |
|---|---|---|
| AI 应用开发 / Agent 开发（工程向） | 辰创 A1 四账本 + A5 Runtime；梵域 B1 Harness + B2 权限 | 辰创 A3、梵域 B5 |
| 行业 Agent / 垂直落地向 | 梵域 B3 知识与 MCP + B1 SOP + B5 Eval + 72 模板 | 梵域 B4 |
| FDE / AI 落地（业务向） | 梵域 B1 SOP + B3 MCP；辰创 A2 销售 Workflow | 辰创 A3、A4 |
| 深挖安全 | 梵域 B2 权限 + 辰创 A4 HMAC + A7 信任阶梯 | 辰创 A1 |

---

## 5. 切换选点时的注意事项

1. **联动收束**：换梵域主选点，`02` 三档口述、`03` 项目二、`07` C 系列要一起改；换辰创主选点，同步 `07` M 系列与 `09` 数字锚点。
2. **同源勿堆叠**：梵域 B4（多后端/上下文）与辰创 A5（Runtime/上下文预算）都讲"上下文"，同场别当两个独立亮点堆。
3. **一次只讲 3–5 个选点**：两个项目各挑 2–3 个，其余留在台账当备用，别把台账全倒出来。
4. **准备度升级规则**：每场面试后按 `08` 回填——答崩的点降为 `⚠`，答得好的升 `✅`。

---

## 6. 红线（禁用表述）

1. **不编**：规模、准确率、效率提升（如"审核效率提升 X%"）——没有第一手口径就不说，给核实路径。
2. **不升格**：Runtime / Harness 底座是团队或可嵌入式开源底座；我做的是**应用端工程化**，不说"我从零写了 Runtime"。
3. **不越界**：梵域 Agent **不替代兽医/管理者**做最终判断；正式制度、SOP、对外材料**必须人工审核**。辰创 Agent 生成建议与话术草稿，**不直接执行不可逆动作**。
4. **不夸大能力**：辰创**不能说"七渠道功能完全一致"**（微信仅 P2P、WhatsApp 逆向协议有风险、QQ 流式仅 C2C、Discord 流式默认关）；Memory 是**结构化知识表 + FTS5**，不是向量 RAG。
5. **不合并威胁模型**：HMAC 一次性签名防的是**伪造与重放**；提示注入是另一套威胁模型，别混讲。
6. **旧叙事不复用**：不再讲"内容生产端 ↔ 获客承接端"的闭环两端叙事。

---

## 7. 材料未覆盖（不许编，被问就说不知道 + 核实路径）

| 缺口 | 现状 |
|---|---|
| 梵域交付状态、使用规模 | 【待确认】见 `00` A1/A2 |
| 辰创交付状态、压测数字、投递成功率 | 【待确认】见 `00` A1/A2/A4 |
| 任何"审核效率提升 / 准确率"数字 | 无第一手口径，**不报** |
| WebSocket 握手与心跳细节 | 全库无来源（wiki 已登记 P0 真缺口），被问到先讲概念再承认未深挖 |
---

## 8. 怎么用这份台账迭代焚诀（沉淀机制）

> 这套库的价值不在"写得多"，而在**每场面试、每次改简历、每段实习之后都留一条痕迹**。下面是把"个人经历"变成"可复用能力"的固定动作。

### 8.1 三个入口：新东西从哪进来

| 入口 | 什么时候发生 | 落到哪 | 沉淀出什么 |
|---|---|---|---|
| **面试** | 每场面完当天 | 先 `08` 记录现象 → 再改 `03`/`06`/`09` | 被问穿的点变成定稿话术；答崩的点降 `⚠` |
| **改简历** | 每次换版本/换主打选点 | 先改 `16` 选点台账 → 再同步 `01`/`02`/`03`/`07` | 新选点进入台账，旧选点降为备用；红线随版本更新 |
| **实习/工作** | 每段新经历结束 | 新开一份"项目故事与选点对照" → 提炼进 `16` | 新项目 → 新选点 → 新 R 点 |

### 8.2 一次迭代的固定顺序（不要跳步）

```text
① 事实先行    拿到新简历/新材料 → 先只提取"事实"（公司/时间/职责/产出/边界），不放结论
② 进台账      把事实写进 `16` 对应项目的"项目事实"，标出不确认的项（→ `00`）
③ 提选点      从事实里挑 3–5 个"能体现技术含金量"的点，写清含金量依据 + 追问准备度
④ 标在用      决定哪几个进简历（✅ 在用），哪几个留台账（⬜ 备用）
⑤ 往下游同步  改 `02`（口述）→ `03`（拷打树）→ `07`（R 点）→ `09`（一页纸）→ `06`（自我介绍，最后动）
⑥ 记一条线    在 `08` 或本文 §9 追加一行"这次改了什么、为什么"
```

### 8.3 三条纪律（防止库自我欺骗）

1. **事实与结论分开写**：事实卡里只写"做了什么、产出了什么"；含金量、价值判断放到选点台账里，一眼能看出哪些是事实、哪些是解读。
2. **准备度要动**：`⚠` 和 `✅` 不是装饰——每场面试验证后必须改；长期全是 `✅` 的台账，说明你没在真面试里被挑战过。
3. **版本要留痕**：换口径时不要删旧的，改成"旧口径 / 新口径"对照（本文 §0 就是范例），否则半年后你自己都说不清哪版是对外口径。

### 8.4 能力沉淀的四个层次（越往下越值钱）

| 层次 | 形式 | 例子 | 可迁移性 |
|---|---|---|---|
| L1 事实 | 我做过什么 | "做过四账本" | 只在这一个项目成立 |
| L2 方法 | 我怎么做的 | "先落库再动手 / 不确定即冻结" | 换项目仍成立 |
| L3 原则 | 我的判断依据 | "模型提议、系统裁决" | 换行业仍成立 |
| L4 世界观 | 我怎么看这件事 | "Agent 可靠性的上限取决于它有多少不可幻觉的验证信号" | 跨领域、可对外输出 |

> 每段经历的价值，是把 L1 逐层往上抽；面试里真正让面试官记住你的，往往是 L3 / L4。`06` §4 的 8 句金句，就是已经抽到 L3 / L4 的那几句。

---

## 9. 难点图索引（2026-09-17 新增）

> 用途：**看懂比背下来更重要**。下面这些图都放在对应文件里，直接在 Obsidian / VSCode / Typora 打开 `.md` 就能看到（Mermaid 原生渲染）。
> ✅ GitHub 网页、本地 `.md`（Obsidian / VSCode / Typora）和**已部署的 `fenjue` 站点**都能看图；站点自 2026-09-17 起支持 Mermaid 渲染，手机 / 平板可左右滑动或点“放大查看”。

### 梵域（养殖合规审查）

| 图 | 放在哪 | 帮你理解什么 |
|---|---|---|
| 多后端 → 统一事件流 | `03` §1.4.2 前 | Claude/Pi 形状不同的事件**收敛成一个** AgentEvent；分清"事件模型"与"传输载体" |
| 权限两层管线（软约束 + 硬拦截） | `03` §1.4.5 | 为什么"提示词不能当安全边界"——上层引导、下层拦截 |
| 审查 SOP 六步 | `03` §1.4.7 前 | 六步里**哪一步查知识、哪一步查数据**；证据不全就停 |
| Memory 写入时序（HMAC + CAS） | `07` M12 | 签名校验、revision 冲突、防重放分别在时序的哪一环 |

### 辰创（多渠道获客）

| 图 | 放在哪 | 帮你理解什么 |
|---|---|---|
| 四账本状态机 | `03` §2.4.3 末 | Inbox 管"输入执行过吗"、Outbox 管"副作用发生了吗"；`uncertain` 为什么必须人工 |
| 串行队列四道闸门 | `03` §2.4.4 前 | 终态/暂停是"不该跑"，活跃/容量是"轮不到你" |

### 两端对比 / 通用概念

| 图 | 放在哪 | 帮你理解什么 |
|---|---|---|
| 两端权限收口位置对比 | `07` 权限哲学对照前 | 梵域拦在**工具门口**、辰创拦在**资源/命令/渠道三层** |
| 上下文三件事（压缩/检索/隔离） | `15` Q5 | 三件事是同一个问题的三个侧面 |
| 五段式组合路由 | `15` Q6 | embedding 只做粗排，"用哪个"vs"允不允许用"分属两层 |
| 意图识别五层漏斗 | `15` Q10 | 从确定到模糊；"能不能做"也是意图的一部分 |

> 已校验：全部 **14 张图**（含 `12` 的 3 张、`13` 的 1 张原有图）用 Mermaid 解析器实测**语法全部通过**。
