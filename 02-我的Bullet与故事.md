# 02 · 我的 Bullet 与故事

> 依据通用模板：`通用焚诀/03-项目故事与拷打/02-故事口述模板·STAR骨架.md`（事实卡 + 三档口述）与 `02-简历与投递/02-Bullet生成与四段式写法.md`。
> 每段经历 = 事实卡 → 四段式 Bullet → 30s / 90s / 3min 三档口述。数字无口径处一律不编（缺口见 `00`）。

---

# 经历一：多渠道获客数字员工平台（对客承接端）

## 1. 项目事实卡（只填事实）

```text
项目名称：多渠道获客数字员工平台（对客承接端）
所属背景/时间：深圳辰创科技实习 · 企业私域客户获客项目 · 2026.02–2026.04
业务用户：客户方市场与销售部门（销售在 IM 里用数字员工承接客户）
原流程与痛点：私域客户分散在企微/微信/社群；线索靠表格登记；跟进无沉淀；
             换个渠道就换个人设，客户体验割裂
项目目标：数字员工长期驻场 7 类 IM，承接触达/破冰/跟进/画像沉淀，消息不重不丢
系统输入：IM 消息（飞书/Telegram/微信/QQ/钉钉/Discord/WhatsApp + Web）
核心链路：消息入站 → 幂等去重 → 身份与权限 → 串行队列 → Agent 执行 → 权限矩阵 → 回复/流式卡片
我的实际职责：核心开发成员，独立负责本 Agent 应用端
协作边界：Pi Agent Runtime 是执行引擎（复用底座）；我负责渠道接入/身份模型/
         可靠性/调度/画像这一层【本人实际参与】
技术与事实清单：
  - 三层模型 Profile/Workspace/Runtime Session + identity_hash【本人实际参与】
  - IMChannel 统一契约（约 50 行最小集合+可选能力方法）+ 前缀 JID 寻址【本人实际参与】
  - 四账本：Inbox（UNIQUE 去重）/ Turn（runId 确定性身份）/ Outbox（幂等键+分阶段状态机）/
    StreamingCard（revision CAS）【本人实际参与】
  - GroupQueue 串行队列：四道入队闸门、指数退避、双时钟、卡死恢复【本人实际参与】
  - /pair 配对码绑定 + 五级信任阶梯（owner_claim_source）【本人实际参与】
已知风险/异常/兜底：sending 后模糊超时 fence 为 uncertain 冻结转人工；
  WhatsApp 个人号逆向协议有封号风险；微信仅 P2P
已知结果：参与交付获客闭环（规模数据【待确认，向导师核实，见 00-A1/A2】）
待确认：交付状态、使用规模、压测数字（见 00）
```

## 2. 四段式 Bullet（口述展开版，每条对应简历一条）

1. **边界**：针对"让模型直接操作业务数据会把身份和可靠性搅在一起"的问题，模型只负责语义理解与表达，身份、权限、可靠性全部收在服务层——触达/破冰/跟进/画像沉淀四段边界由此切开。
2. **三层模型**：针对多销售团队共用平台、身份与数据边界易串的问题，设计 Profile（身份与策略）/ Workspace（文件系统隔离边界，folder 名正则约束防路径穿越）/ Runtime Session（执行记录）三层 + identity_hash 可审计传播 + 四段人设（IDENTITY/SOUL/AGENTS/TOOLS）+ 三层上下文预算。
3. **渠道与可靠性**：针对七渠道协议异构、内核怕被 SDK 绑死的问题，抽象约 50 行 IMChannel 最小契约 + 前缀寻址，能力差异做成可选方法分级适配（不强行抹平）；消息走四账本状态机：inbox 全局去重、turn_runs 以 runId 创建即去重、outbox 按幂等键分阶段持久化、流式卡片 revision CAS；sending 之后模糊超时不重试、fence 为 uncertain 冻结转人工裁决。
4. **画像沉淀**：针对跨会话线索无长期存储、不可信的问题，设计带并发控制的画像记忆，写入用 HMAC 签名一次性校验、防伪造防重放、抗提示注入污染。

## 3. 口述三档

### 30 秒电梯版
> 一家企业的私域客户散在七个 IM 里，销售跟进靠表格、换个渠道人设就断。我做了对客承接端的数字员工：以 Agent Runtime 为引擎，数字员工长期驻场七类 IM，身份用三层模型隔离，消息用四账本状态机保证不重不丢，跟进沉淀成带 HMAC 校验的客户画像——和内容生产端一起跑通"内容→触达→线索"获客闭环。

### 90 秒标准版
> 痛点（30s）：客户方销售团队在企微、微信、社群多点触达客户，线索登记靠表格、跟进不沉淀，多团队共用一套系统时身份和数据边界特别容易串。
> 我做的（40s）：我负责对客承接端。第一，从获客流程拆出触达、破冰、跟进、画像沉淀四段，模型只做语义理解和表达，身份权限可靠性收在服务层；第二，设计 Profile、Workspace、Runtime Session 三层模型，身份变更用 identity_hash 做可审计传播，人设拆成四段约束配三层上下文预算；第三，七个 IM 平台走统一 IMChannel 契约加前缀寻址，能力差异分级适配——微信仅私聊、Discord 流式默认关闭都如实建模；消息可靠性做四账本状态机，回执丢失走人工裁决而不是盲目重试。
> 结果与兜底（20s）：同一个数字员工可以同时驻场销售群和客户私聊；画像带 HMAC 防伪造防重放。规模数据我手头没有第一手口径，可以会后跟您对齐我们实际的度量方式。

### 3 分钟深讲版（追问展开点）
在 90 秒版基础上，按面试官追问方向展开：
- **展开身份三层**：为什么要三层（身份/边界/记录分离，Session 刻意不叫 sessions 防止被当成第二个产品级 Agent）；identity_hash 由四段提示词+运行策略+名称共同计算、配递增 version。
- **展开四账本**：每张账本的唯一键（inbox 的 provider+account+external_message_id、turn 的 runId=sha256(logicalKey)、outbox 的 turn_run_id+ordinal+payload_hash）；"先落库再动手 / 不确定即冻结 / 恢复尊重租约"三句哲学；sending 后超时为什么不能当失败重试（Provider 可能已收到，副作用不可知）。
- **展开并发**：GroupQueue 按 Workspace folder 划串行车道，四道入队闸门（终态丢弃/变更暂停/活跃检查/容量检查），指数退避，双时钟判卡死，分级终止（_close 2s→docker stop 15s→强杀）。
- **展开绑定**（OneID 雷点，见 `03` Q1 定稿话术）：/pair 配对码 + 五级信任阶梯 + 粘性归属。

### 白板架构图（被要求画图时，按这个顺序画——先业务后 AI）

```text
【第一笔：业务角色与数据流】
销售/客户（7 类 IM + Web）
   │  ①消息入站（前缀 JID 寻址 → Inbox 账本去重）
   ▼
【第二笔：接入与安全层】im-manager 连接池 + IMChannel 统一契约
   │  ②安全前置：处理锁 + inboundAllowed 门控 + scopeChannelJid
   ▼
【第三笔：调度层】GroupQueue 串行车道（四道闸门/指数退避/双时钟）
   │  ③按 {folder}#{agentId} 串行化
   ▼
【第四笔：执行层】Agent（容器/宿主双模式，stdin/stdout/IPC 三通道）
   │  ④执行边界：服务层 ACL 矩阵 + Owner 门控 + 渠道准入
   │    （注：工具执行权限保持开放，权限收口在资源/命令/渠道层）
   ▼
【第五笔：投递层】Outbox 分阶段状态机 → sendMessage 写回渠道
   │  ⑤流式卡片（revision CAS 单活跃卡 + 三级降级）
   ▼
【第六笔：沉淀层】客户画像（HMAC 签名写入）+ Workspace Memory
```
> 讲法：先画①⑤（业务闭环：消息进来、回复出去、画像沉淀），再点亮中间的技术模块；我负责的部分在②③④⑤（标注【本人实际参与】），执行引擎是团队底座。

---

# 经历二：蓬蓬AI · 员工超级助手（内容生产端）

## 1. 项目事实卡

```text
项目名称：蓬蓬AI · 员工超级助手（内容生产端）
所属背景/时间：杭州梵域智能实习 · 八戒农牧（农牧企业）AI 工作平台 · 2026.05–2026.08
业务用户：客户方（八戒农牧）市场与运营团队及各岗位员工（桌面端 Electron + Web 端双端使用）
原流程与痛点：多岗位产出靠人工整理；模板与行业知识分散；各端直连模型 SDK，换一家模型改一片代码；
             接企业系统方式散、凭据各配一套
项目目标：多端可复用的企业 AI 工作助手，换模型不动页面；沉淀查/算/写/讲 72 个工作模板，
         接入 8 大行业知识库；产出物料进入获客数字员工分发
核心链路：多端输入 → 统一 RPC → AgentBackend（Claude/Pi）→ AgentEvent 流 →
         权限拦截 → 工具执行（MCP/API/8 大知识库 RAG）→ 双端渲染
我的实际职责：核心开发成员，独立负责本 Agent 应用端
协作边界：执行引擎复用 Pi Agent Runtime 底座；我负责模型后端抽象/事件流/多端通信/
         权限/能力接入层【本人实际参与】
技术与事实清单：
  - AgentBackend 统一模型后端：ClaudeAgent/PiAgent 收敛为 provider-agnostic 流式事件接口
  - AgentEvent 可辨识联合类型：文本增量/工具启停/权限请求/错误/完成
  - 统一 RPC：Electron IPC 与 WebSocket 双传输复用同一套会话与模型连接逻辑
  - safe/ask/allow-all 权限模式 + PermissionManager 统一裁决（`evaluateToolCall()`）+ PreToolUse 管线
  - MCP/API/本地数据 + 8 大知识库（RAG 检索）统一能力接入层；契约一致性测试校验前后端通道
已知风险/兜底：流式渲染错误恢复行为双端一致；敏感工具先确认再执行
已知结果：双端复用同一套 Agent 能力；八戒农牧 72 模板落地（使用规模【待确认，见 00-A2】）
待确认：交付状态、使用规模（见 00）
```

## 2. 四段式 Bullet（口述展开版）

1. **统一模型后端**：针对各端直连模型 SDK、换一家改一片的问题，抽象 AgentBackend，把 Claude 与 Pi 在事件模型、子进程通信、工具代理上的差异收敛成一个流式事件接口——界面只认一种事件，调模型、调推理强度不改页面。
2. **AgentEvent 统一事件流**：针对双端两套连接、事件各自处理会漂移的问题，用可辨识联合类型把文本增量、工具启停、权限请求、错误、完成各成一种事件——双端增量渲染与错误恢复行为一致。
3. **会话隔离与工具权限**：针对多名运营并行使用状态互相污染、提示词约束不可靠的问题，按会话隔离工作区，工具调用前统一走 Safe/Ask/Allow-All 权限模式与集中拦截——写盘等敏感操作必须先确认。
4. **能力接入与防漂移**：针对接企业系统方式散、凭据各配一套的问题，做统一能力接入层把 MCP、API、本地数据统一转成 Agent 可调用工具；契约一致性测试把多端接口漂移拦在开发期。

## 3. 口述三档

### 30 秒电梯版
> 农牧企业八戒农牧多岗位产出靠人工整理、模板与行业知识分散，各端还直连模型 SDK，换模型要改一片代码。我做了内容生产端（蓬蓬AI 员工超级助手）：抽象统一模型后端，Claude 和 Pi 收敛成一个流式事件接口，换模型不改前端；双端走统一 RPC 和 AgentEvent 事件流，行为不漂移；沉淀查/算/写/讲 72 个工作模板、接入 8 大行业知识库，工具执行有硬权限，敏感操作必须先确认。产出的经营材料与营销物料直接进获客数字员工分发。

### 90 秒标准版
> 痛点（30s）：八戒农牧市场运营要在多个平台出内容，人工写、素材散、模板和行业知识分散；产品有桌面端和 Web 端两套连接各管各的；换模型或接企业系统成本很高。
> 我做的（40s）：我负责 Agent Runtime 应用侧。第一，抽象 AgentBackend 统一模型后端，把 Claude 和 Pi 两类后端在事件模型、子进程通信、工具代理上的差异收敛成一个流式事件接口；第二，用可辨识联合类型统一 AgentEvent 事件流，文本增量、工具启停、权限请求、错误、完成各成一种事件，Electron 和 Web 复用同一套 RPC 与渲染逻辑；第三，多名运营并行使用，按会话隔离工作区，工具调用前统一走 Safe/Ask/Allow-All 三档权限加集中硬拦截；第四，把八戒农牧 8 大知识库（养殖/疫病/饲养标准/政策法规）经 RAG 检索，连同 MCP、API、本地数据统一成一个能力接入层，并用契约一致性测试把多端接口漂移拦在开发期。
> 结果与兜底（20s）：核心目标是"换模型不用改前端、多端同一套 Agent 能力"，同时沉淀了查/算/写/讲 72 个工作模板——目标在架构上是成立的、契约测试在守；具体使用规模我需要跟团队确认口径后再给您数字。

### 3 分钟深讲版（追问展开点）
- **展开 AgentBackend**：为什么要抽象（UI 层不该耦合模型 SDK）；事件如何统一（后端 SDK 事件映射到统一事件类型）；Thinking Level、模型配置如何随抽象流转（capabilities-driven UI）。
- **展开权限**：为什么提示词约束不够（模型可以被打动）；权限硬控怎么分层——PermissionManager `evaluateToolCall()` 按 safe/ask/allow-all 三档裁决，`runPreToolUseChecks()` 六步管线二次把关（模式检查→源阻断→前置条件→call_llm 拦截→输入转换→ask 提示决策）；DANGEROUS_COMMANDS 强制审批、写盘在 safe 模式硬编码拦截。
- **展开契约测试**：测什么（通道名、参数与返回结构对齐 ElectronAPI）；为什么 IPC 特别容易漂移（前端 API、IPC 通道、后端 handler 三处各自演进）。
- **展开与经历一的串联**：生产端产出的物料与话术进入获客数字员工完成触达分发——闭环是业务上的，两套系统不共享数据库。

### 白板架构图（被要求画图时）

```text
【多端】Electron 桌面端 ──┐
                        ├─→ 统一 RPC（IPC / WebSocket 双传输复用同一套逻辑）
Web 端 ─────────────────┘        │
                                 ▼
                    AgentBackend 统一模型后端
                    ├─ ClaudeAgent（适配 Claude SDK 事件）
                    └─ PiAgent（进程外 JSONL RPC + 工具代理）
                                 │
                                 ▼
                    AgentEvent 统一事件流（可辨识联合）
                    文本增量 │ 工具启停 │ 权限请求 │ 错误 │ 完成
                                 │
                                 ▼
                    PermissionManager 权限裁决（safe/ask/allow-all + DANGEROUS_COMMANDS 强制审批）
                    + runPreToolUseChecks 六步管线
                                 │
                                 ▼
                    能力接入层（MCP / API / 本地数据 → 统一工具）
                                 │
                                 ▼
                    契约一致性测试（Channel Map ↔ ElectronAPI，开发期拦漂移）
```
> 讲法：从"换模型不改前端"这个目标倒着讲——界面只认 AgentEvent 一种事件 → 后端差异收敛在 AgentBackend → 传输差异收敛在统一 RPC → 漂移由契约测试守。四个收敛点讲完，架构自洽。

---

# 时间线：两段企业实习的成长线（防"散活"拷打）

```text
经历一（2026.02 起 · 辰创 · 承接端）         经历二（2026.05 起 · 梵域 · 生产端）
────────────────────────────            ────────────────────────────
解决"多人/多渠道怎么可靠"：                复用并升级为"多端/多模型怎么统一"：
  IMChannel 统一渠道契约                    AgentBackend 统一模型后端
  四账本 + runId fencing                    AgentEvent 事件流 + 统一 RPC
  ACL 权限矩阵 + Owner 门控                  Safe/Ask/Allow-All + PermissionManager
  /pair 绑定 + 信任阶梯                      契约测试防漂移 + 8 大知识库 RAG
────────────────────────────            ────────────────────────────
能力演进主线：可靠接入 → 统一抽象 → 「接入、执行、控险、稳跑」完整闭环
```

**3 分钟串联讲述 · 企业实习完整闭环故事（背这段）：**
> 我这两段企业实习，串起来是同一个获客闭环的两个端。先在深圳辰创做对客承接端，核心问题是"多人、多渠道怎么可靠"——统一 IMChannel 渠道契约、四账本状态机保证消息不重不丢、/pair 绑定加信任阶梯让身份可审计，数字员工能长期驻场七个 IM；后到杭州梵域做内容生产端，把同一套 Agent 平台落地农牧企业八戒农牧（蓬蓬AI 员工超级助手），问题升级成"多端、多模型怎么统一"——AgentBackend 统一模型后端、AgentEvent 统一事件流，沉淀查/算/写/讲 72 个工作模板、接入 8 大行业知识库，换模型不改前端。生产端产出的经营材料、营销物料与话术，进入承接端完成触达分发和画像沉淀，跑通"内容生产 → 多渠道触达 → 线索沉淀"的完整获客闭环。串起来看，我一直在做同一件事：让 Agent 能力可以被组合、被信任、被放进真实业务流程稳定运行。两条线合起来，正好覆盖了 AI 应用从内容生产到触达分发的完整链路。
