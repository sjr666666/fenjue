# 02 · 我的 Bullet 与故事

> 依据通用模板：`通用焚诀/03-项目故事与拷打/02-故事口述模板·STAR骨架.md`（事实卡 + 三档口述）与 `02-简历与投递/02-Bullet生成与四段式写法.md`。
> 口径来源：`简历/石敬荣_Agent开发_Skill场景版.md`（2026-09-16 最新）。选点总台见 `16-项目故事与选点台账.md`。
> 每段经历 = 事实卡 → 四段式 Bullet → 30s / 90s / 3min 三档口述。数字无口径处一律不编（缺口见 `00`）。
> ⚠️ 旧版"内容生产端 ↔ 获客承接端"闭环叙事已停用；两段是**独立的企业交付**，串法见 `16` §3。

---

# 经历一：蓬蓬AI · 养殖场合规与异常审查 Agent（杭州梵域）

## 1. 项目事实卡（只填事实）

```text
项目名称：蓬蓬AI · 养殖场合规与异常审查 Agent
所属背景/时间：杭州梵域智能科技实习 · 八戒农牧企业 AI 助手 · 2026.05–2026.08
业务用户：养殖企业的运营/生产/经营等多岗位（含场长、防疫、财务、公关等 6 类部门）
原流程与痛点：用药、防疫、消毒、死亡、环境记录分散在客户制度与现场资料里；
             现场问题依赖人工交叉核查，效率低、易漏；不同模型后端与桌面/Web 端行为易漂移；
             敏感操作缺硬边界；Agent 行为缺可回归的验证
项目目标：把高频岗位工作转成可复用的 Agent 工作模板；让模型能按任务调用证据，
         同时保证敏感操作可控、会话互不污染、行为可复现可回归
系统输入：客户制度、养殖知识、业务数据、每日记录
核心链路：读取每日记录 → 核对库存与用药 → 检查死亡和环境异常 → 对照防疫规范
         → 汇总关联证据 → 输出风险报告
我的实际职责：核心开发成员，负责本 Agent 应用端（Harness 生命周期 / 工具接入层 /
             权限拦截 / 多后端适配 / 上下文装配 / Eval 回归）
协作边界：执行底座（Pi / Claude Agent SDK 一类可嵌入式 Harness）复用；我做的是应用端工程化
技术与事实清单：
  - Agent Harness：BaseAgent 生命周期统一技能解析、前置条件、分支上下文注入、消息重写
  - 工具接入层：MCP / API / 本地数据源 → 统一工具契约（注册 / 凭据注入 / 结果回传）
  - 权限：Safe / Ask / Allow-All 三档 Permission Mode + PreToolUse 集中硬拦截
  - 多后端：AgentBackend 统一契约 + 可辨识联合 AgentEvent（文本增量/工具启停/权限/错误/完成）
  - 上下文：Session / Workspace 隔离 + 动态装配权限规则、客户资料、AGENTS.md
  - Eval：Event Trace 可回放 + Channel Map 契约测试 + 工具冒烟用例
已知结果：交付查/算/写/讲 4 类任务 × 6 类部门共 72 个工作模板；
         形成生物安全、成本分析、生产复盘、政府申报等成果样例；完成客户资料与部署交接
边界：Agent 出风险报告与正式材料初稿，不替代兽医/管理者做最终判断；
     正式制度、SOP 与对外材料需人工审核；Eval 是确定性 Trace + 契约 + 冒烟，不是 LLM Judge
待确认：交付状态、使用规模（见 00-A1/A2）
```

## 2. 四段式 Bullet（口述展开版，每条对应简历一条）

1. **养殖知识与 MCP Tools**：针对"养殖规范、客户制度、现场资料和经营数据分散，全塞进 Prompt 会膨胀、过期、来源不可追"的问题，把知识检索、业务数据读取、计算分析、成果生成统一成工具契约，通过 MCP/API/本地数据源接入层完成注册、凭据注入与结果回传——新增数据源只需实现适配与契约，不改 Agent Loop。
2. **Agent Harness / 异常审查 SOP**：针对"每个岗位写一套大 Prompt 会重复流程与失败分支、还容易漏关键核查步骤"的问题，把审查拆成"读取每日记录 → 核对库存与用药 → 检查死亡和环境异常 → 对照防疫规范 → 汇总关联证据 → 输出风险报告"，用 `BaseAgent` 生命周期统一技能解析、前置条件、上下文注入和消息重写——通用骨架复用，岗位差异只放在模板、领域资料和工具组合。
3. **Tool Calling / 人工审批**：针对"Agent 读取经营资料、生成文件、提处置建议时的权限风险，光靠提示词'谨慎'不构成安全边界"的问题，Permission Mode 进系统提示词做软约束，每次实际调用前再由 `PreToolUse` 集中检查做硬拦截——查询和报告草稿可自动执行，正式制度、SOP、对外材料必须人工审核后发布。
4. **多后端与上下文工程**：针对"Claude/Pi 事件、通信、工具代理和错误路径不同，多端各自解析会漂移"的问题，用 `AgentBackend` 收敛后端差异、可辨识联合 `AgentEvent` 统一事件语义；Session/Workspace 隔离工作目录、会话状态和工具上下文，并约束上下文文件发现范围、大小与缓存。
5. **Agent Eval / Bad Case 回归**：针对"只测函数返回值看不出 Agent 有没有正确完成工具调用、权限判断、事件收尾和审查流程"的问题，把文本、工具调用、权限、错误、完成状态记录成可回放 Event Trace；用 Channel Map 契约测试与工具冒烟用例覆盖库存与用药不一致、防疫记录缺失、异常死亡、环境变化未触发提示等失败模式——先评测可确定行为，能定位是模型、工具还是协议引入回归。

## 3. 口述三档

### 30 秒电梯版
> 八戒农牧的用药、防疫、消毒、死亡和环境记录散在不同资料里，现场问题靠人工交叉核查，容易漏。我参与做养殖场合规与异常审查 Agent：把审查流程拆成"读取记录→交叉核对→识别异常→对照规范→汇总证据→出风险报告"的可执行 Workflow，用统一 Harness 让 6 类部门 72 个岗位模板复用同一执行骨架，敏感操作走权限硬拦截、正式材料人工审核，并用 Event Trace + 契约测试做 Bad Case 回归。

### 90 秒标准版
> 痛点（30s）：八戒农牧的制度文件、养殖知识和经营数据分散，员工要人工查资料、做核对、写报告；用药、防疫、消毒、死亡和环境记录一分散，现场异常就得靠人工交叉核查，效率低而且容易漏。
> 我做的（40s）：我负责这个 Agent 应用端。第一，把审查 SOP 拆成六步可执行流程，用 BaseAgent 生命周期统一技能解析、上下文注入和工具编排，岗位差异只落在模板与工具组合；第二，把养殖知识、业务数据和成果生成统一成工具契约，通过 MCP/API/本地数据源接入，让 Agent 按任务调证据而不是靠 Prompt 里的死知识；第三，权限做两层——Permission Mode 进提示词是软约束，PreToolUse 在每次调用前硬拦截；第四，用 AgentBackend 和统一 AgentEvent 收敛多后端与桌面/Web 端的差异。
> 结果与兜底（20s）：交付查/算/写/讲 4 类任务 × 6 类部门共 72 个工作模板，形成生物安全、成本分析、生产复盘等成果样例；Eval 用可回放 Event Trace + 契约测试 + 冒烟用例，覆盖库存对不上、防疫缺失、异常死亡、环境恶化未提示这些 Bad Case。规模类数字我手头没有第一手口径，可以会后跟您对齐我们的度量方式。

### 3 分钟深讲版（追问展开点）
- **展开 Harness**：为什么不是把 Prompt 写长（每岗位重写流程/工具/失败分支 → 漏步、难维护）；`BaseAgent` 生命周期统一了什么（技能解析、前置条件、上下文注入、消息重写）；为什么是模板方法不是普通继承（差异点在"谁执行模型调用"）。**边界**：`AGENTS.md` 是声明性规则，不是独立 Workflow 引擎。
- **展开工具接入**：为什么不是把客户资料塞进系统提示词（膨胀、过期、来源不可追）；工具返回结构化结果 + 来源上下文；**边界**：MCP 解决能力接入，不自动保证资料正确。
- **展开权限**：为什么提示词约束不够（模型可能判断错、被上下文污染、被注入影响，且"可被说服"）；`PreToolUse` 六步管线（模式检查→源阻断→前置条件→call_llm 拦截→输入转换→ask 提示决策）；Safe 模式对写盘硬编码拦截。**边界**：这套不是数据库多租户 ACL 的替代品。
- **展开 Eval**：先评测可确定行为——事件顺序、工具契约、权限路径、错误收尾、多端一致性，再检查业务 SOP 是否走完；Trace 是定位证据，契约测试是回归门禁，冒烟用例是端到端可用性。**边界**：不是 LLM-as-a-Judge，没有虚构准确率；Gold/Bad/Ambiguous Dataset 是扩展方向。
- **展开多后端**：适配难点不是换模型名，而是统一事件、工具和失败语义；上层消费一种事件流，差异留在 Adapter；可辨识联合让 UI 按事件类型穷举处理，不把权限或错误吞成普通文本。

### 白板架构图（被要求画图时，先业务后 AI）

```text
【第一笔：业务与 SOP】
养殖场每日记录 / 库存用药 / 死亡环境 / 防疫规范
   │  ①合规审查六步：读取记录→核对→识别异常→对照规范→汇总证据→出报告
   ▼
【第二笔：能力接入层】MCP / API / 本地数据源 → 统一工具契约
   │  ②知识检索 / 业务数据读取 / 计算分析 / 成果生成
   ▼
【第三笔：Harness 执行骨架】BaseAgent 生命周期
   │  ③技能解析 → 前置条件 → 上下文注入 → 消息重写 → 委托后端执行
   ▼
【第四笔：多后端与事件】AgentBackend（Claude / Pi）
   │  ④Adapter → AgentEvent 统一事件流
   │    （文本增量 │ 工具启停 │ 权限请求 │ 错误 │ 完成）
   ▼
【第五笔：权限硬拦截】PermissionManager（Safe/Ask/Allow-All）
   │  ⑤PreToolUse 六步管线：查询/草稿自动，写盘/对外材料人工审核
   ▼
【第六笔：Eval 兜底】Event Trace 可回放 + Channel Map 契约测试 + 工具冒烟
                    （库存不一致 / 防疫缺失 / 异常死亡 / 环境未提示）
```
> 讲法：先画①（业务闭环），再点亮②③④⑤（能力→执行→多端→权限），最后收在⑥（怎么知道它对不对）。四个收敛点（工具契约、Harness 骨架、事件流、权限）讲完，架构自洽。

---

# 经历二：多渠道获客数字员工平台（深圳辰创）

## 1. 项目事实卡（只填事实）

```text
项目名称：多渠道获客数字员工平台
所属背景/时间：深圳辰创科技实习 · 企业私域客户获客项目 · 2026.02–2026.04
业务用户：客户方市场与销售部门（销售在 IM 里用数字员工承接客户）
原流程与痛点：私域客户分散在企微/微信/社群；线索靠表格登记；跟进无沉淀；
             换渠道就换人设，客户体验割裂；回执可能丢失、消息可能重复、跨会话数据污染
项目目标：数字员工长期驻场 7 类 IM，串联客户触达、最近互动、机会与风险识别、
         关键人梳理、Next Best Action 与长期画像沉淀；消息不重不丢
系统输入：IM 消息（飞书/Telegram/微信/QQ/钉钉/Discord/WhatsApp + Web）
核心链路：消息入站 → 幂等去重 → 身份与权限 → 串行队列 → Agent 执行 → 权限矩阵 → 回复/流式卡片
我的实际职责：核心开发成员，负责本 Agent 应用端
协作边界：Pi Agent Runtime 是执行引擎（复用底座）；我负责渠道接入/身份模型/
         可靠性/调度/画像这一层
技术与事实清单：
  - 销售 Workflow：Account → 最近互动 → 业务变化 → Opportunity → Risk → Stakeholder → Next Best Action
  - IMChannel 统一契约（约 50 行最小集合 + 可选能力方法）+ 前缀 JID 寻址
  - 四账本：Inbox（UNIQUE 去重）/ Turn（runId 确定性身份）/ Outbox（幂等键 + 分阶段状态机）/
    StreamingCard（revision CAS）
  - GroupQueue 串行队列：四道入队闸门、指数退避、双时钟、卡死恢复
  - 四段 Prompt（IDENTITY/SOUL/AGENTS/TOOLS）+ 预检/运行期监控/自动压缩
  - Workspace Memory：事实/决策/经验/待办四类知识 + FTS5 + revision CAS + HMAC 一次性签名
已知风险/异常/兜底：sending 后模糊超时 fence 为 uncertain 冻结转人工；
  WhatsApp 个人号逆向协议有封号风险；微信仅 P2P
已知结果：形成可跨渠道复用的获客数字员工执行链路（规模数据【待确认，见 00-A1/A2】）
```

## 2. 四段式 Bullet（口述展开版，每条对应简历一条）

1. **销售 Agent Workflow**：针对"多渠道获客最难的不是收到消息，而是销售无法从碎片化互动里持续判断机会、风险、关键人和下一步动作"的问题，把流程拆为 `Account → 最近互动 → 业务变化 → Opportunity → Risk → Stakeholder → Next Best Action`；渠道消息、会议纪要、历史跟进先归一化为客户 Timeline，再由 Agent 提取事实、判断阶段、识别延期或流失信号——模型给判断和草稿，服务端守身份、权限和副作用。
2. **多渠道互动归一化**：针对"七个 IM 平台连接方式、身份格式、流式能力、消息长度都不同，主链直接依赖各平台 SDK 会让每加一个渠道都要重写"的问题，抽象约 50 行 `IMChannel` 最小契约 + 前缀 JID 寻址，把流式卡片等差异建模为可选方法按能力降级（不强行抹平）。
3. **可靠性四账本**：针对"一条消息从接收到发送期间进程可能在任意位置崩溃，系统必须同时回答'这条输入是否执行过'和'这个外部副作用是否已经发生'"的问题，用 Inbox 全局去重、Turn 确定性 `runId`、Outbox 分阶段幂等 + payload hash、StreamingCard revision CAS；**sending 之后模糊超时不重试**，fence 为 `uncertain` 冻结整个 Turn 转人工。
4. **Account Memory 与安全写入**：针对"客户画像要跨 Session 复用，但聊天历史不能等同于长期知识，且 Agent 不该持有可任意写库的永久凭据"的问题，把沉淀为事实/决策/经验/待办四类知识（带状态、重要度、置信度、有效时间），用 FTS5 检索历史 Timeline；写入走 revision CAS 防并发覆盖、幂等键防重复、HMAC-SHA256 一次性能力签名防伪造防重放。
5. **Agent Runtime 与上下文预算**：针对"长生命周期 Agent 需要冷启动任务、持续流式、中途追发和外呼工具，单管道扛不住；人设与规则混在长 Prompt 里职责不清、版本难审计"的问题，以 Pi Runner 承载 Agent Loop（stdin/stdout + 文件系统 IPC 管理任务/跟发/中断），会话串行队列避免同客户并发；人设拆 IDENTITY/SOUL/AGENTS/TOOLS 四段，配预检、运行期监控和自动压缩控制上下文。

## 3. 口述三档

### 30 秒电梯版
> 一家企业的私域客户散在七个 IM 里，销售跟进靠表格、机会状态接不上。我做了获客数字员工：以 Agent Runtime 为引擎，数字员工长期驻场七类 IM，把"客户互动 → 机会判断 → 风险识别 → 关键人梳理 → 下一步跟进"做成可执行的销售 Workflow；渠道走统一契约，消息走四账本状态机保证不重不丢，跟进沉淀成带 HMAC 校验的客户画像。

### 90 秒标准版
> 痛点（30s）：客户方销售在企微、微信、社群多点触达客户，线索登记靠表格、跟进不沉淀；客户互动一分散，机会状态和下一步动作就接不上，跨渠道还容易串身份和数据。
> 我做的（40s）：我负责这个 Agent 应用端。第一，把销售 SOP 拆成可执行 Workflow——汇总互动、识别 Opportunity、判断流失或延期风险、梳理关键人、生成下一步动作，模型只做语义理解和表达，身份权限可靠性收在服务层；第二，七个 IM 走统一 IMChannel 契约加前缀 JID，能力差异分级适配（微信仅私聊、Discord 流式默认关闭都如实建模）；第三，消息可靠性做四账本状态机——inbox 全局去重、turn 按确定性 runId 创建即去重、outbox 分阶段幂等、流式卡片 revision CAS，回执丢失冻结转人工裁决而不是盲目重试。
> 结果与兜底（20s）：客户跨渠道跟进能延续，画像用 FTS5 检索、写入带 HMAC 一次性签名防伪造防重放。规模数据我手头没有第一手口径，可以会后跟您对齐我们实际的度量方式。

### 3 分钟深讲版（追问展开点）
- **展开销售 Workflow**：Opportunity 怎么判断（最近互动、业务变化、当前诉求、异议、关键人状态、历史承诺形成阶段性判断，保留证据来源）；为什么需要 Timeline（同客户可能跨渠道跨 Session 联系，单轮上下文留不住长期状态）；Next Best Action 是否自动执行（默认只给建议或话术草稿）。
- **展开渠道契约**：为什么用 JID 而不是两个字段（同时承载 provider/chat/account/thread 正交维度，索引、路由、权限共用一种地址语言）；新增渠道要改什么（实现连接工厂 + 契约 + 流式控制器，不改主链）。
- **展开四账本**：每张账本的唯一键（inbox 的 provider+account+external_message_id、turn 的 runId=sha256(logicalKey)、outbox 的 turn_run_id+ordinal+payload_hash）；"先落库再动手 / 不确定即冻结 / 恢复尊重租约"三句哲学；sending 后超时为什么不能当失败重试（Provider 可能已收到，副作用不可知）；为什么不是全部 at-least-once（外部发送是不可逆副作用，结果未知必须停下）。
- **展开 Memory**：为什么不用聊天历史直接当 Memory（临时信息多 + 注入风险，长期记忆应是提炼、带来源、可修订的知识）；revision CAS / 幂等键 / HMAC 各解决什么（并发覆盖 / 重复写入 / 伪造与重放）；**边界**：HMAC 防伪造防重放，不判断内容本身是否真实，也不等于防提示注入。
- **展开 Runtime 与上下文**：四段 Prompt 为什么要拆（职责清晰、版本可审计）；三层上下文预算（静态预检 → 启动按模型窗口计算 → 运行中按比例压缩）。
- **展开调度**（备用）：GroupQueue 按 Workspace folder 划串行车道，四道入队闸门（终态丢弃/变更暂停/活跃检查/容量检查），指数退避，双时钟判卡死，分级终止。

### 白板架构图（被要求画图时，先业务后 AI）

```text
【第一笔：业务角色与数据流】
销售/客户（7 类 IM + Web）
   │  ①消息入站（前缀 JID 寻址 → Inbox 账本去重）
   ▼
【第二笔：接入与安全层】im-manager 连接池 + IMChannel 统一契约
   │  ②安全前置：处理锁 + inboundAllowed 门控 + scopeChannelJid
   ▼
【第三笔：调度层】GroupQueue 串行车道（四道闸门/指数退避/双时钟）
   │  ③按 {folder}#{agentId} 串行化
   ▼
【第四笔：执行层】Agent（容器/宿主双模式，stdin/stdout/IPC 三通道）
   │  ④销售 Workflow：互动 → Opportunity → Risk → Stakeholder → Next Best Action
   │    （服务层 ACL 矩阵 + Owner 门控 + 渠道准入）
   ▼
【第五笔：投递层】Outbox 分阶段状态机 → sendMessage 写回渠道
   │  ⑤流式卡片（revision CAS 单活跃卡 + 三级降级）
   ▼
【第六笔：沉淀层】客户画像（HMAC 签名写入）+ Workspace Memory
```
> 讲法：先画①⑤（业务闭环：消息进来、回复出去、画像沉淀），再点亮中间的技术模块；我负责的部分在②③④⑤，执行引擎是团队底座。

---

# 时间线：两段企业实习的成长线（防"散活"拷打）

```text
经历一（2026.02 起 · 辰创 · 对客执行可靠性）      经历二（2026.05 起 · 梵域 · 行业合规判断）
────────────────────────────              ────────────────────────────
解决"多人/多渠道怎么可靠"：                 解决"行业 SOP 怎么变成 Agent 能力"：
  IMChannel 统一渠道契约                      BaseAgent 异常审查 SOP 骨架
  四账本 + runId fencing                      MCP / API / 本地数据 → 统一工具契约
  ACL 权限矩阵 + Owner 门控                    Safe/Ask/Allow-All + PreToolUse 硬拦截
  /pair 绑定 + 信任阶梯                        AgentBackend + AgentEvent 多后端统一
  画像 Memory + HMAC                          Event Trace + 契约测试 + 冒烟 Bad Case 回归
────────────────────────────              ────────────────────────────
能力演进主线：横向的可靠性执行 → 纵向的行业深度 →「接入、执行、控险、稳跑」完整闭环
```

**3 分钟串联讲述（背这段，见 `16` §3 同款）：**
> 我这两段企业实习，是同一条主线的两次演练——把 Agent 放进真实业务流程，解决它怎么接入、怎么执行、怎么控风险、怎么稳定运行。深圳辰创那段做的是对客执行的可靠性：数字员工驻场 7 类 IM，我做渠道归一、身份与权限、四账本可靠投递、会话串行调度和画像记忆，核心是"多人、多渠道怎么不重不丢、不越权"。杭州梵域那段做的是行业场景的合规判断：给八戒农牧做养殖场合规与异常审查 Agent，把用药、防疫、消毒、死亡、环境记录串成"读数→核对→识别异常→对照规范→汇总证据→出报告"的审查 Workflow，同一套 Harness 复用到 6 类部门 72 个岗位模板，核心是"行业 SOP 怎么变成可执行、可观察、可回归的 Agent 能力"。串起来看，一次练的是横向的可靠性，一次练的是纵向的行业深度——合起来正好是 Agent 能不能真正落地到真实业务要同时具备的两面。