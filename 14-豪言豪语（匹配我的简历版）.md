# 14-豪言豪语（匹配我的简历版）

> 依据：飞书 wiki《面试豪言豪语（耄耋整理）》（往里豪）为**观点来源**，逐条改造为**我的简历口径**；与 `06-我的高频Q&A与金句.md`、`13-Agent系统选型原则与演进.md` 互查。
> **往里豪原话**部分为《往里豪.pdf》逐字完整收录（原文编号 1,2,4,5,6,7,8,9，原文缺第 3 题；原话中"我"均为往里豪本人，仅供理解思路）。
> 往里豪取材说明（原话）：内容整理自群友讨论、群主抖音作品、飞书资料及个人面试经历。本文保留原有观点与编号，仅进行版式整理，便于复习、分享与打印。
> 往里豪核心主线（原话）：
> 把业务需求真正落地，并通过交互设计、工具选择和架构品味，把复杂需求拆解成清晰、可验证、用户愿意使用的工程路径。
>
> 红线（背之前必读）：
> 1. **往里豪的经历数字（微调 70%→95%、几百条标注、几十块钱、pi-core 400 行、25+ 事件钩子）都不是我的**——涉及这些时按 `06` §3.7 诚实四步，绝不冒认。
> 2. 简历 `小石_Agent开发_Skill场景版.md`（2026-09-16 最新）是唯一口径；旧版 `立即到岗` 仅在对方拿旧版时用。我的事实是「两段企业实习交付：梵域 · 八戒农牧养殖场合规与异常审查 Agent / 辰创 · 多渠道获客数字员工平台，我负责两个 Agent 应用端，执行底座复用成熟开源/可嵌入 Harness（Pi 一类）」。
> 3. 每条"我的版"括号里标注证据出处（简历/06/13 对应位置），无证据就不说。
> 4. 只改话术，不动简历事实；面试先背 `06` §4.5 已有的 6 句豪言，本文是它们的**可展开弹药**。

---

## 手机复习顺序（渐进式披露）

1. **第一层：一页纸**。进场前只扫下面九条，确认每题的结论和边界。
2. **第二层：我的版**。被追问时按题目展开，先说自己的口径和证据。
3. **第三层：观点来源**。只有想补推导、练反问或核对边界时，再看往里豪原话；原话不是我的经历，绝不直接背成自己的。

## 一页纸（九条 · 我的版一句话）

1. **职业规划**：不是 title，是从"把需求实现出来"走向"能把业务需求落地成高使用率工程方案"——我已在两个企业 Agent 应用端上验证这个方向。
2. **TS+React**：AI 应用状态复杂（流式/事件/上下文），类型即契约、双端共享类型，编译期拦住不一致。
3. **Python？**：数据科学与模型训练仍是 Python，Agent 应用层与前后端一体化，TS 生态更完整。
4. **Pi/Agent SDK 选型**：复用成熟 Runtime 底座（Pi），差异收敛到边界里，上层产品/Runtime/权限/审计自己做。
5. **RAG vs LLM Wiki**：RAG 是"查询时检索"，LLM Wiki 是"摄入时编译"；我的场景用 RAG 解决"先检索再生成"，LLM Wiki 是学到的方法论，不是我的第一手生产。
6. **规模与一致性**：检索方案按场景定规模——我的知识库结构化 + 关键词检索已够用，诚实讲边界，不假装有向量能力。
7. **输出不对≠微调**：Prompting First, Fine-Tuning Last——先用 Prompt，再上 RAG/上下文注入，微调是最后手段。
8. **微调危害**：灾难性遗忘、知识固化、迭代长、可解释性差。能用上一层解决，绝不动下一层。
9. **选型总原则**：场景匹配 > 定制需求 > 核心能力；先看行业已验证的方案，不追热点。

---

## 逐条：往里豪原话（完整）→ 我的版本（每条都挂简历证据）

### 1. 职业规划

**往里豪原话**〔原版第 1 题：你的职业规划是什么〕：

> 我对职业发展的思考，是从一个观察出发的：初级工程师往往停留在"把需求实现出来"，而高级工程师的核心标志，是能不能把业务需求转化成一个可落地、并且用户使用率高的工程方案 - 也就是 Harness 设计的能力。
>
> 这中间的鸿沟其实不在技术本身，而在于几个更综合的判断：一是交互设计，方案好不好用，用户愿不愿意用；二是工具选择，是不是选对了杠杆；三是在用户看得见的东西和看不见的东西之间做平衡，这本质上是架构品味。
>
> 所以我的职业规划，不是某个固定的 title，而是一条能力成长的路径：在实际业务场景中不断验证和迭代自己，朝着"能把业务需求真正落地"这个方向走。短期来看，我希望在当前的业务里把每一个方案都做深做透；长期来看，我希望自己成为那个能把复杂需求拆解成清晰工程路径的人。

**我的版（与固定自我介绍模板衔接，不冲突）**：
> 我的职业规划不是盯着某个 title，而是一条能力成长路径：先把"把需求实现出来"做扎实，再往"把业务需求落地成用户愿意用的工程方案"走。判断的维度我认同是三个——交互设计（用户用不用）、工具选择（选对杠杆）、架构品味（可见与不可见的平衡）。短期在业务里把每个方案做深，长期成为能把复杂需求拆成清晰工程路径的人。

**证据/衔接**：与 `06` §1 自我介绍模板主线一致（把 Agent 放进真实业务流程）；最后一句可接固定模板，不另起炉灶。
**【无需核实】观点类。**

### 2. 项目为什么用 TS + React

**往里豪原话**〔原版第 2 题：项目为什么用 TS + React〕：

> 技术选型我一般不会只看个人偏好，而是先看行业里已经被验证过的方案。我调研开源项目的时候注意到一个现象：DeepSeek 的 Harness 项目，还有国外很多高星的 AI 应用框架，几乎清一色是 TypeScript + React。
>
> 我觉得这不是巧合，背后有几个实际原因：
>
> 第一，类型安全的刚需。AI 应用和普通 CRUD 应用不一样，它有大量的中间状态 - 流式响应、工具调用链、多轮对话上下文，状态非常复杂。TypeScript 的类型系统能在编译期就拦住很多状态不一致的问题，这对维护性很关键。
>
> 第二，生态的向心力。现在 AI 前端的核心工具链，比如 Vercel AI SDK、LangChain JS，都是优先支持 React 和 TypeScript 的。你用别的框架不是不能做，但很多东西要自己造轮子，文档和社区支持也差一截。
>
> 第三，全栈的一致性。很多 AI 应用是 Next.js 全栈做的，前后端都是 TS，数据模型可以共享类型，这对快速迭代很重要。
>
> 所以对我来说，选 TS + React 不是跟风，而是基于对真实项目的观察和对技术生态的判断，选了一条验证过、生态最完整的路。

**我的版（换成我的项目事实）**：
> 不是我选了 TS+React，是我在项目里被"状态复杂"逼出来的。两个端（桌面+Web）共用一套 Agent 能力，中间态有流式增量、工具启停、权限请求、错误恢复，双端渲染还得一致。我用可辨识联合类型把 AgentEvent 收敛成封闭集合，双端 switch 穷尽；加上前后端共享一套 DTO、契约测试拦漂移——这些全靠类型系统兜住，没有类型就是运行时炸。生态是加分项，但我的第一理由是"类型即契约"。

**证据**：简历项目二「多端连接与渲染统一」+「外部能力接入与契约防漂移」；`06` §3.5「为什么 TypeScript 全栈？」与 §2.6 双端行为不一致事故。
**【无需核实，纯我的事实】**

### 3. 那 Python 呢（简短备答）

**往里豪原话**〔原版第 4 题：那 Python 呢？〕：

> Python 在数据科学和模型训练层仍然是主流，但在 Agent 应用层和前后端一体化的场景下，TypeScript 的优势越来越明显。

**我的版**：
> Python 的强项在数据科学和模型训练，这块我的项目不涉及；到了 Agent 应用层、尤其是双端交互和事件流工程，TypeScript 的类型约束和全栈一致性优势更明显。Coding 项目我用 TS，但模型侧的 Python 生态我保持关注，不排斥。

**【无需核实】**

### 4. 为什么用 Pi / Agent SDK，不自研或套壳

**往里豪原话**〔原版第 5 题：怎么不用 LangChain、LangGraph，而转用 Pi Agent SDK？〕：

> 这个选型我是实际对比过的，不是拍脑袋。最开始我用的是 LangGraph，因为它生态最大、文档最全。但用了一段时间后发现几个问题：
>
> 第一，TypeScript 支持是二等公民。LangGraph 的主线是 Python，TS 版是移植过来的，每个版本通常晚 4 到 8 周，遇到 bug 或者新特性，TS 这边要等。我们整个技术栈是 TS，这一点很别扭。
>
> 第二，抽象层太重。LangGraph 是一个非常底层的图编排框架，你要定义 node、edge、typed state、reducer，还要管 checkpointing。做一个简单的 ReAct Agent 都要写不少样板代码，团队上手成本高。而且它和 LangChain 的耦合比较深，很多东西要顺着 LangChain 的抽象来。
>
> 第三，性能和可控性。LangGraph 的状态传递是通过共享 channel + reducer 的方式，层级深了之后状态管理会变得复杂，调试也不太直观。
>
> 后来我调研到 Pi Agent SDK，它的设计哲学完全不一样 - 极简、runtime-agnostic。核心 pi-agent-core 只有四百多行代码，做的事情很纯粹：agent loop、工具调用、状态管理、事件流。它有一个我很喜欢的设计：应用层消息和 LLM 层消息是分离的，扩展的时候非常清晰。而且它是 TS 原生的，不是移植版，25 多个事件钩子可以很精细地控制 agent 行为。这种生态使得非常适合在其之上进行二次开发。
>
> 当然我不是说 LangGraph 不好 - 如果需要复杂的持久化工作流、human-in-the-loop、暂停恢复这些能力，LangGraph 确实更强。但我们的场景更看重开发效率和灵活性，Pi 的轻量设计让我们能把逻辑做得更复杂的同时，代码反而更简洁。

**我的版（不把 LangGraph 冒认成我的对比经历，用我的真实选型）**：
> 我的决策是"复用底座、控制上层"，理由分两层：
> 下层复用：Runtime 底座用团队选定的 Pi/Agent SDK——执行循环、Tool Calling、事件这些成熟能力不重复造轮子，因为 Harness 不是我们的核心竞争力（这句来自我的选型原则，见 13 号文件成立）。
> 上层自控：渠道、身份、权限、消息可靠性、事件契约都在应用端自己做——梵域那段把 Claude 与 Pi 两类后端在事件模型、子进程通信、工具代理上的差异收敛成一个流式事件接口，换模型不改页面；权限走 Safe/Ask/Allow-All + 六步管线。
> 如果面试官追问"为什么不选 LangGraph/LangChain"：**不说"我用过并踩过坑"**，而是说"架构上 LangGraph 更重、更面向复杂工作流与持久化，我的场景要的是一致性收敛与快速迭代，Pi 的极简底座更合适；具体对比我没有第一手踩坑数据，这是基于设计哲学的判断，不是拉踩"。

**证据**：简历项目二「模型层统一，换模型不动页面」「会话隔离与工具权限」；`13` 号文件选型原则；`06` §3.5「为什么复用 Pi Agent Runtime 不自研执行循环」。
**【需核实】"400 行/25+ 事件钩子/LangGraph 晚 4–8 周"是往里豪数字，我不报；若想报，先回 pi-agent-core 源码核对。**

### 5. 为什么不用 RAG 用 LLM Wiki

**往里豪原话**〔原版第 6 题：为什么不用 RAG 而用 LLM Wiki？〕：

> 这个问题我想从两个范式的本质区别说起。
>
> Graph RAG 是"查询时检索"范式。它把文档抽成实体和关系三元组，构建知识图谱，每次查询时遍历子图、现场组装上下文给 LLM。这个方案的优势很明显：多跳推理能力强，能沿着关系链找到向量检索找不到的关联；可扩展性好，能处理百万级文档。但它的问题也很突出：第一，构建成本高，实体抽取、关系抽取、图谱构建和维护每一步都很重；第二，有研究表明 Graph RAG 在很多真实任务上反而不如普通 RAG，因为图谱构建的误差会传递到检索结果；第三，知识不沉淀，每次查询都像第一次接触这些材料一样重新检索、重新理解，token 效率低。
>
> LLM Wiki 是"摄入时编译"范式，这是 Karpathy 今年 4 月提出来的。核心思路就一句话：不要在查询时检索，要在摄入时编译。新资料进来时，LLM 就把它读懂、整理成结构化的 wiki 页面，自动维护交叉引用和一致性，后续查询直接读相关页面。它有三个我很看重的优势：第一，知识是持久化积累的，越用越丰富，不会每次从零开始；第二，查询时读的是完整页面而不是碎片化的 chunk，上下文更完整；第三，矛盾和冲突在编译阶段就被解决或标记了，不会像 RAG 那样把冲突的 chunk 同时塞给 LLM。
>
> 当然我不是说 LLM Wiki 万能。如果是大规模、多用户、高频更新的企业知识库，Graph RAG 的可扩展性还是有优势的。但我们的场景是中小规模的领域知识库，资料相对稳定，更看重知识的深度整理和沉淀，LLM Wiki 的编译模式在这个场景下效率和质量都更优。这是一个基于场景的选型，不是追热点。

**我的版（我的第一手是 RAG/检索，不要被带偏）**：
> 我的业务场景里真正跑的是一套检索方案：梵域养殖合规审查 Agent 接企业知识库，先检索素材再生成，不凭空编——审查链路里"对照防疫规范"那一步就是先查规范再判断。LLM Wiki"摄入时编译"是方法论层面我认可的方向：知识持久化沉淀、查整页、矛盾在编译期解决，适合中小规模且稳定的领域知识库。但**诚实边界**：LLM Wiki 在我的项目里不是第一手生产，我不会把没跑过的东西说成跑过；如果面试官问，我讲两层——一层是我实际做的 RAG/关键词检索工程，一层是我对"查询时检索 vs 摄入时编译"两种范式的理解，并标明后者来自学习。

**证据**：简历项目二「外部能力统一接入」（企业知识库、模板、MCP 转 Agent 工具）；`06` §2 Q2「RAG 先查再答」；`06` §3.5「检索为什么用 FTS 关键词不是向量」= 诚实边界范例。
**【需核实】只有我对 LLM Wiki 要按"学习材料"口径讲，不能冒充项目经历。**

### 6. 数据规模大了怎么办 / 一致性怎么保证

**往里豪原话**〔原版第 7 题：那数据规模大了怎么办，LLM Wiki 怎么做更新后的一致性？〕：

> LLM Wiki 适合 100-200 篇文章、5-10 万 token 以内的知识库，超过这个规模确实需要 RAG 来兜底。我们的场景在这个范围内，而且可以用分层加载（L0 -> L1 -> L2 -> L3）来控制上下文窗口。
>
> 一致性上 LLM Wiki 有 raw/ 层存原始资料（只进不出），wiki/ 层由 LLM 维护，新资料进来时会触发增量编译，更新相关页面并维护交叉引用。schema/ 层定义了页面结构和规则，保证一致性。而且每个知识条目都锚定到原始来源，可追溯。

**我的版（没有生产规模就讲方法论 + 我的实际规模）**：
> 规模设计的前提是先量量化再选型。我的知识库是结构化 + 关键词检索（FTS），单机低并发够用；如果知识规模上量、需要语义关联检索，再评估向量库或 RAG。分层加载（L0→…→L3 控制上下文窗口）和"原始层只进不出、编译层增量更新、结构层定义规则"这套一致性设计我认同为方法论，但我不报往里豪的具体数字——我的实际规模要按简历口径（结构化知识库 + FTS，无 5–10 万 token 大库）讲，超了就诚实说"这块我没有第一手规模数据"。

**证据**：简历专业技能「SQLite、Docker」；`06` §3.5「为什么 SQLite」「检索为什么是 FTS」。
**【需核实】凡涉及"我的知识库多少篇/多少 token"，先数真实数据再报，否则不报。**

### 7. 输出不对，是不是就需要微调

**往里豪原话**〔原版第 8 题：输出不对，是不是就需要微调？〕：

> 这个问题我想先反驳一个前提："输出不对就需要微调"，这个认知其实是有问题的。我在实际项目里的做法是，先做问题诊断，再选手段，而不是一上来就微调。
>
> 我对模型优化有一个三层的层级认知：
>
> 第一层是 Prompt Engineering。这是成本最低、迭代最快的手段。大部分输出质量问题，通过优化系统提示词、few-shot 示例、输出格式约束就能解决。行业里有个共识叫"Prompting First, Fine-Tuning Last" - 大多数团队以为需要微调的问题，其实是提示词写得不够好。
>
> 第二层是 RAG 或上下文注入。如果问题是模型缺少知识 - 比如不知道企业内部文档、最新政策、产品信息 - 这时候应该用 RAG，而不是微调。因为微调是把知识"烧"进模型权重里，更新一次就要重新训练；RAG 是查询时动态检索，知识库更新即时生效。一句话总结：RAG 解决"模型不知道什么"，微调解决"模型知道但怎么做"。
>
> 第三层才是 Fine-tuning。只有当前两层都试过了，问题确实是行为层面的 - 比如需要非常特定的输出风格、领域术语的内化、高流量场景下用小模型微调来降低推理成本 - 这时候才上微调。而且 2026 年了，微调也不是全量训练，主流是 LoRA/QLoRA + SFT，需要对齐偏好时再加 DPO，单卡 4090 就能跑 7B 模型，成本已经很低了。
>
> 错误使用微调的危害是真实的：第一，灾难性遗忘，微调后模型的通用能力可能下降；第二，知识固化，微调进去的信息很难更新和删除；第三，迭代周期长，改 prompt 五分钟，重新微调要几天；第四，可解释性差，出了问题不知道是数据的问题还是训练的问题。
>
> 所以我的选型原则是：能用上一层解决的，绝不动下一层。微调是武器，不是默认选项。

**我的版（方法论可直接用，我的实践落在前两层）**：
> 我的原则是"能用上一层解决的，绝不动下一层"：第一层先把 Prompt 与输出契约做对——我在项目里把输出收敛成可辨识联合类型与 Schema，约束的是"结构稳定"；第二层知识缺失走检索/上下文注入（RAG 即时生效，不用重训）；第三层才是微调，解决"知道但做不好"的行为问题，且今天主流是 LoRA/QLoRA+SFT。微调的四个代价——遗忘、固化、迭代慢、不可解释——决定了它必须是最后手段，不是默认选项。**我目前的第一手实践落在前两层**，微调本身没有冒认经历。

**证据**：简历「输出契约/工具权限/质量迭代」；`06` §2 Q3 降幻觉（依据→约束→校验→兜底）。
**【无需核实 as 方法论；"微调我做过"不可说】**

### 8. 那你实际微调过吗

**⚠️ 最危险的一题——往里豪的经历数字绝不能背**

**往里豪原话**〔原版第 9 题：那你实际微调过吗？〕：

> 调过。之前做过一个场景，prompt 怎么调格式都不稳定，后来用 QLoRA + SFT 微调了一个 7B 模型，几百条标注数据，训练成本几十块钱，格式稳定性从 70% 提到了 95% 以上。但那是在确认了 prompt 和 RAG 都解决不了之后才做的。

**我的版（诚实四步，答案向回切我的主场）**：
> 微调这个动作我目前没有第一手实战，我不会冒认。我完整做过的是它的**前置诊断链路**：先验证 Prompt/契约能否解决，再验证检索注入能否补知识，确认都不行才会进入微调评估——这个决策顺序我在真实项目里反复走。微调的技术细节（LoRA/QLoRA、SFT、偏好对齐）和代价模型我学习过、能讲清楚，但训练实践我没有生产证据，这块我不硬讲。

**追问预案**：若面试官追问"怎么确定该微调"，回 `06` §3.7 四步 + 回溯第三板斧（先 Eval 定位是"缺材料、推理不闭合、还是行为固化"——第三板斧的回流方向就是微调问题的诊断框架）。
**【需核实】不可引用 70%→95%/几十块/几百条，那些是往里豪的。**

---

## 使用说明

1. **先背 `06` §4.5 的 6 句豪言**（那已是你的），本文 9 条是"被追问到底"时的展开弹药。
2. **往里豪原话只借思路，不借经历**：原话中所有"我"字开头的句子和数字都是他的，必须能在简历或 `06` 找到证据的才进"我的版"。
3. 与 `13-Agent系统选型原则与演进.md` 同构：13 讲"选型原则 + 演进"，本文讲"逐题拆解"，两篇互不重复。
4. 消化后可将最顺手的条目并入 `06`，但 `06` 的定稿句优先保持稳定，新句必须标注证据出处再补。
